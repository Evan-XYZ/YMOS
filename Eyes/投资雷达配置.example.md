# 投资雷达配置示例

> 可选文件。复制为 `Eyes/投资雷达配置.md` 后修改；不复制时使用 SOP 默认值。

| 配置 | 可选值 | 当前值 |
|:---|:---|:---|
| 默认模式 | `balanced` / `price` / `event` | `balanced` |
| 历史窗口 | 1-7 个自然日 | 7 |
| 价格对象 | `holdings_and_watchlist` / `named_only` | `holdings_and_watchlist` |
| 热门报告 | `auto` / `off` | `auto` |
| A 股 | `on` / `off` | `on` |
| 美股 | `on` / `off` | `on` |

## 我的额外事件源（可留空）

-

## 说明

- 当前方向、跨市场映射和阅读偏好统一维护在 `持仓与关注/当前关注方向与投资偏好.md`，不要在运行配置里重复维护。
- 不要把 API Key、账户号码、真实仓位或成本写进本文件；Key 只放 `.env`，持仓只放状态机。
- 临时模式以用户本次暗号为准，例如“今天跑事件优先雷达”。
