# 进阶指南

> YMOS 帮你从投研的重复劳动中解放出来——数据收集、信号追踪、报告生成交给 AI，
> 你专注于三件最重要的事：**打磨策略**、**做关键判断**、**深度调研**（自己读研报、挖行业，把材料拖入个股文件夹供 AI 后续分析）。
>
> 开箱即用只需 RSS（零 API Key）。以下是让系统能力持续进化的路径。

---

## 前提：先跑通最小闭环

在启用任何进阶功能之前，请确保你已经：

1. ✅ 成功运行 `跑一下市场洞察`（使用 RSS 路径）
2. ✅ 成功运行 `跑一下投资雷达`（价格扫描正常）
3. ✅ 看到本地生成的报告文件（`Eyes/` 下）

> 三模块架构、暗号速查、日常运行流程详见 `README.md`。

---

## Level 1：Finnhub Free API Key（推荐）

### 它能带来什么

| 能力 | 无 Finnhub Key | 有 Finnhub Key |
|:---|:---|:---|
| 美股/Crypto 价格 | Yahoo Finance 回退 | Finnhub 实时报价 + Yahoo 互备 |
| 英文新闻 | 仅 RSS 源 | + Finnhub 持仓匹配新闻 |
| P15 触发检测 | 无 | 自动标记财报/并购等关键事件 |
| 个股新闻 | 无 | `company-news` 按 Ticker 精准拉取 |

### 如何获取

1. 访问 [finnhub.io](https://finnhub.io/) 注册免费账号
2. 在 Dashboard 中复制你的 API Key
3. 编辑 `.env` 文件：
   ```
   FINNHUB_API_KEY=你的key
   ```

### 验证

```bash
# 测试价格查询
python3 Eyes/scripts/fetch_price_api.py --symbols "AAPL,NVDA" --output /tmp/test_price.json

# 测试新闻拉取
python3 Eyes/scripts/fetch_finnhub_news.py --hours 24 --output /tmp/test_news.json
```

### 免费 Key 的限制

- 每分钟 60 次 API 调用
- 仅覆盖美股和 Crypto（A股/港股不支持）
- 行情数据有 15 分钟延迟
- 个股新闻窗口最长 1 年

> 对于 YMOS 的使用场景（日频扫描 + 事件触发），免费 Key 完全够用。

---

## Level 1+：Tushare Token（A 股用户推荐）

### 它能带来什么

| 能力 | 无 Tushare | 有 Tushare |
|:---|:---|:---|
| A 股价格（.SS/.SZ） | Yahoo Finance 回退（延迟高、偶尔不稳定） | Tushare 实时报价 + Yahoo 互备 |
| A 股日线数据 | 无 | 支持历史日线查询 |

### 如何获取

1. 访问 [tushare.pro](https://tushare.pro/) 注册账号
2. 在个人中心获取 Token
3. 编辑 `.env` 文件：
   ```
   TUSHARE_TOKEN=你的token
   ```

### 免费 Token 的限制

- 注册并完善个人信息后获得 120 积分（不消耗），达到日常接口调用的使用门槛
- 部分高级接口需要更高积分等级
- 仅覆盖 A 股（.SS/.SZ），美股/港股/Crypto 走 Finnhub 或 Yahoo

### 三源价格路由机制

YMOS 的价格扫描自动分流（`Eyes/scripts/fetch_price_router.py`）：

```
Ticker 后缀判断：
  .SS / .SZ → Tushare（无 Token 时回退 Yahoo）
  .HK       → Yahoo Finance
  其他       → Finnhub（无 Key 时回退 Yahoo）
```

> Finnhub 和 Tushare 可独立配置，互不影响。建议：美股用户配 Finnhub，A 股用户配 Tushare，两市都看就都配。

### Finnhub 可用能力速查

| API | 端点 | YMOS 用途 |
|:---|:---|:---|
| 实时报价 | `/quote?symbol=AAPL` | 投资雷达价格扫描 |
| 个股新闻 | `/company-news?symbol=AAPL&from=...&to=...` | P4 关注点更新 |
| 大盘新闻 | `/news?category=general` | 市场洞察补充数据 |
| 基础指标 | `/stock/metric?symbol=AAPL&metric=all` | P1/P4 基本面数据 |
| 同行对比 | `/stock/peers?symbol=AAPL` | P1 竞争格局 |
| 财报日历 | `/calendar/earnings?from=...&to=...` | 事件触发预警 |
| 市场状态 | `/stock/market-status?exchange=US` | 价格扫描时间判断 |

---

## Level 2：Market Data API（yongmai.xyz）

### 它能带来什么

一个已经清洗、分类、去重的中英文市场事件数据源。相当于把 28 个 RSS 源的抓取 + CIO 处理全部在服务端完成，一个 API 请求就拿到结构化的半成品情报。

| 维度 | 纯 RSS | Market Data API |
|:---|:---|:---|
| 数据源数量 | 15 个免费 RSS | 28 个（含 RSSHub 搭建类） |
| 预处理 | 需要本地消耗 Token 处理 | 服务端已完成清洗与分类 |
| 中文源 | 几乎都需要RSSHUB搭建中转 | 原生支持高质量中文资讯与研报 |
| 输出格式 | 原始 RSS JSON | 直接可用的结构化 Markdown |

### 如何获取
请注意：这不是一个商业化的 SaaS 产品。 这是我（勇麦）个人实盘每天在用的底层数据管道。我没有精力、也不打算提供任何“保姆级”的客服支持。

因此，我不做公开推介。但如果你满足以下条件：

- 已经跑通了 YMOS 最小闭环（使用免费的纯 RSS 路径）；
- 清楚知道自己需要更高质量、更低噪音的数据输入。
- 具备基础的调试能力或者愿意跟随你的agent稍耐心配置。


你可以直接前往我的博客 https://yongmai.xyz/register-2 注册账户。在用户中心，你可以自助开通并获取专属的 API Key。


### 配置
在你的 .env 文件中填入你在网站后台生成的API密钥：
```env
YMOS_MARKET_API_URL=https://yongmai.xyz/wp-json/tib/v1/reports
YMOS_MARKET_API_KEY=你的key
```

- 或者有定制化的投资系统需求

可以通过以下方式联系：
- 📧 邮箱：evan@yongmai.xyz
- 📱 公众号：勇麦

**前提**：请先确保你已经用纯 RSS 路径跑通了完整的市场洞察→投资雷达→策略分析闭环。

---

## RSS 信息源自定义

### 当前默认源

YMOS 内置 15 个免费 RSS 源，分为 5 个分类：

| 分类 | 数量 | 主要源 |
|:---|:---|:---|
| 美股 | 3 | Bloomberg Markets, CNBC Markets, FT Markets |
| 宏观 | 2 | CNBC Finance, The Economist |
| 科技 | 5 | Bloomberg Tech, FT Tech, TechCrunch, Nvidia Blog, The Verge |
| Crypto | 1 | Bloomberg Crypto |
| 深度洞察 | 4 | Seeking Alpha, Stratechery, Bloomberg Opinion, TechInAsia |

### 如何自定义

编辑 `Eyes/scripts/rss_sources.json`，按以下格式增减源：

```json
{
  "name": "你的源名称",
  "url": "https://example.com/feed/",
  "category": "美股",
  "priority": "medium",
  "note": "简要说明"
}
```

### 补充 RSS：与 Market Data API 并行使用

如果你已经配置了 Market Data API（Level 2），但还想额外订阅一些 API 覆盖不到的 RSS 源（如特定行业博客、独立分析师），可以创建补充 RSS 配置：

1. 复制 `Eyes/scripts/rss_sources_custom.json.example` 为 `rss_sources_custom.json`
2. 填入你的自定义 RSS 源（格式与 `rss_sources.json` 一致）
3. 市场洞察 SOP 会自动检测此文件，存在即拉取，不存在即跳过

**补充 RSS 与主数据源独立运行**——无论你用 API 还是默认 RSS 作为主数据源，补充 RSS 都会额外拉取并作为 P13 分析的补充输入。

```bash
# 补充 RSS 的调用方式（SOP 自动处理，通常不需要手动执行）
python3 Eyes/scripts/fetch_rss.py 1 \
  --config Eyes/scripts/rss_sources_custom.json \
  --output "Eyes/市场洞察/Raw_Data/YYYY-MM/supplementary_rss_YYYYMMDD.json"
```

### 更多源参考

完整的 28 源清单（含需 RSSHub 搭建的中文源）：
- 🔗 https://yongmai.xyz/tib-rss-list/
- 包含：金十数据、华尔街见闻、财联社、东财研报等中文源
- 这些源需要自行搭建 [RSSHub](https://docs.rsshub.app/deploy/) 服务

---

## Level 3：更多金融数据 API（自定义集成）

YMOS 的脚本层是模块化的——每个数据源一个独立脚本，统一输出 JSON。你可以参考现有的 `Eyes/scripts/fetch_finnhub_news.py` 或 `Eyes/scripts/fetch_price_api.py` 的模式，接入任何金融数据 API。

核心模式很简单：**一个端点 + 一个 API Key → 输出 JSON → 喂给 SOP 处理链**。

### 推荐的金融数据服务商

| 服务商 | 免费层 | 主要能力 | 适合场景 |
|:---|:---|:---|:---|
| **Tiingo** | 有 | 美股历史行情、新闻、IEX 实时数据 | 替代/补充 Finnhub 行情 |
| **Alpha Vantage** | 有 | 美股/外汇/Crypto 行情、技术指标 | 技术分析、回测数据 |
| **Polygon.io** | 有限 | 美股实时/历史行情、期权链 | 精准行情、期权策略（P10） |
| **Benzinga Pro API** | 付费 | 实时新闻、评级变动、内部交易 | 高频事件驱动 |

### 如何接入新数据源

1. **创建脚本**：`Eyes/scripts/fetch_[source_name].py`
   - 参考 `Eyes/scripts/fetch_finnhub_news.py` 的结构
   - 从 `.env` 读取 API Key
   - 输出标准 JSON 到 `Eyes/` 对应目录

2. **更新 `.env.example`**：
   ```
   TIINGO_API_KEY=
   ALPHA_VANTAGE_KEY=
   ```

3. **更新 SOP**：在对应 SOP 中添加可选的数据拉取步骤（参考 `Eyes/SOP_市场洞察.md` 的 Step 2 模式）

4. **Agent 自动识别**：只要脚本存在且 API Key 配置了，SOP 步骤会自动执行

### 社媒监控（高阶）

对于需要实时舆情监控的用户，可以集成社交媒体 API：

| 平台 | API | 用途 |
|:---|:---|:---|
| **X (Twitter)** | [X API](https://docs.x.com/x-api/introduction) | 财经 KOL 动态、市场情绪监控 |
| **Reddit** | Reddit API | WSB/投资社区热点追踪 |

社媒数据可以作为 P13 市场洞察的补充信号源，或者作为 P4 关注点雷达的"叙事维度"输入。

> 提示：社媒数据噪音极高，建议先通过 CIO 提示词（`Brain/references/cio-rss-*.md`）做一轮清洗再喂给 P13。

---

## 投资策略诊断：审视你的投资逻辑

> 在优化策略之前，先确认你的投资逻辑本身没有问题。

YMOS 内置了一个独立的投资策略诊断模块（`Brain/ymos-diagnosis/`），它不做策略制定、不做交易执行，只做一件事：**帮你审视整体投资逻辑和系统健康度**。

### 什么时候用

| 场景 | 推荐模式 |
|:---|:---|
| 刚接触 YMOS，不确定自己的投资策略是否合理 | 体检模式 → 梳理清楚再填写投资偏好 |
| 连续亏损或大幅回撤后 | 体检模式 → 检查是策略问题还是执行问题 |
| 对某个具体投资决策拿不准 | 问诊模式 → 先判断问题本身成不成立 |
| 每季度定期审视 | 体检模式 → 7 项检验，看看有没有新的漏洞 |
| 策略调整前 | 体检模式 → 确认调整方向是否合理 |

### 怎么用

让 AI 读取诊断入口文件：

```
请读取 [你的YMOS路径]/Brain/ymos-diagnosis/SKILL.md

诊断一下我的策略
```

AI 会引导你选择问诊或体检，然后一步步带你走完。

### 和 P 系列提示词的区别

| | 诊断模块 | P 系列提示词 |
|:---|:---|:---|
| **做什么** | 审视整体投资逻辑 | 执行具体投资分析 |
| **关注点** | 你的系统有没有漏洞 | 这只股票该不该买/卖 |
| **产出** | 诊断报告 / 问题消解 | 策略报告 / 买卖建议 |
| **频率** | 按需 / 定期 | 每日 / 事件驱动 |

两者互补：诊断模块帮你确认**方向对不对**，P 系列帮你**走对的路**。

> 诊断模块也作为独立开源项目发布：[github.com/Evan-XYZ/ymos-diagnosis](https://github.com/Evan-XYZ/ymos-diagnosis)，不需要安装 YMOS 也能单独使用。

---

## 策略进化：P 系列提示词定制

> 这是最核心的进阶——不是接更多数据源，而是**让大脑变聪明**。

### P 系列提示词是什么

`Brain/references/` 下的 P1-P18 提示词是 YMOS 的投资决策框架。它们代表一种经过验证的投资思路和决策路由：

| 阶段 | 提示词 | 职责 |
|:---|:---|:---|
| **建档** | P1 Genesis | 基石档案（治理/壁垒/财务/周期） |
| **追踪** | P4 Radar | 五维度关注点雷达 |
| **定性** | P2 Phase Check | PVE/PVP 阶段判断 |
| **事件** | P3 Event Impact | 事件冲击分析 |
| **买入** | P5 FOMO Killer | 买入前审计 |
| **持有** | P6 Profit Keeper | 持仓评估 |
| **组合** | P7 Portfolio Check | 仓位再平衡 |
| **宏观** | P8 Macro Filter | 宏观压力测试 |
| **估值** | P9 Valuation | 反向 DCF 估值 |
| **期权** | P10 Options | 期权策略 |
| **复盘** | P11 Autopsy | 交易复盘 |
| **纪律** | P12 Referee | 纪律审查（最终关卡） |
| **市场** | P13 Market Scan | 市场全景扫描 |
| **板块** | P14 Sector | 热门板块挖掘 |
| **深度** | P15 Deep Insight | 事件深度洞察 |
| **财报** | P16 Earnings | 财报深度解读 |

### 如何定制

P 系列提示词是**框架样板**。真正的进阶是结合你自己的投资策略来改造它们：

1. **修改权重和判断标准**：比如 P5 FOMO Killer 中的评分阈值
2. **增加行业特定逻辑**：比如 P1 中加入你熟悉行业的特殊指标
3. **调整风险偏好**：P12 纪律审查中的红线规则
4. **增加新的 P 模块**：遵循相同命名规范，SOP 中增加路由即可

> ⚠️ 修改提示词前，建议先用原版跑通几轮完整流程，理解每个 P 模块在决策链中的角色。

### SOP 路由定制

五大策略路由（买入/加仓/持有/卖出/再平衡）定义在 `Brain/SOP_策略分析.md` 中。你可以：
- 调整每条路由的 P 链顺序
- 增加条件分支（如特定市值区间走不同路由）
- 增加新的路由类型

---

## Agent Skills 与跨模块联动

### Agent 能力自检

YMOS 设计为可被不同 Agent 编排产品驱动（Claude Code、ChatGPT Codex、Google Antigravity、OpenClaw 等）。首次接入时，应让 Agent 明确检查四类能力；**仅把文件放进目录不会自动完成这一步**：

| 能力类别 | 检查内容 | 影响的环节 |
|:---|:---|:---|
| **Web 搜索** | 能否实时搜索互联网？ | P1 建档、P2 阶段判断、P3 事件分析 |
| **工具/Skills** | 是否挂载了 MX_Skills 或其他数据工具？ | 选股筛选、财务数据、宏观数据 |
| **文件系统范围** | 能否读取 YMOS 同级目录（如你自己的笔记/研究目录）？ | 上下文补充、跨模块联动 |
| **代码执行** | 能否运行 Python 脚本？ | 数据拉取（Eyes/scripts/*.py） |

有什么能力就用什么能力——缺少某项能力不会阻塞主流程，只是分析质量会有差异。

### Agent 可增强的环节

以下是 Agent 的额外能力可以提升质量的具体环节：

| SOP 环节 | 增强方式 | 约束 |
|:---|:---|:---|
| **P1 Genesis（建档）** | 用搜索补充公司财务、竞争格局、行业数据 | 不改变 P1 模板结构 |
| **P2 Phase Check** | 搜索最新价格/资金流数据辅助判断 | 不改变 PVE/PVP 判断标准 |
| **P3/P15 事件分析** | 搜索最新新闻、公告替代手动输入 | 不改变分析框架 |
| **P13 市场扫描** | 若有财经搜索工具 → 补充中文财经资讯 | 不替代 RSS 主链，作为补充 |
| **选股筛选** | 若有选股工具 → P13/P14 发现主题后自动筛选候选池 | 输出须转入 P1 建档流程 |
| **财务快照** | 若有财务数据工具 → P1 建档时批量拉取指标 | 写入个股基础知识库 |
| **宏观数据** | 若有宏观工具 → P8 压力测试前获取最新数据 | 使用标准化格式 |

### 系统架构比喻

- **大脑**：Brain/（P 系列提示词 + 策略路由）— 做决策、出结论
- **眼睛**：Eyes/（市场洞察 + 投资雷达 + 价格扫描）— 看市场、报信号
- **记忆**：持仓与关注/（状态机 + 个股文件夹）— 存状态、积累上下文
- **手脚**：Skills 工具（财经搜索、Web 搜索等）— 获取数据、执行查询
- **感官增强**：同级知识模块（你自己的笔记库等）— 打破数据墙，丰富上下文

### 跨模块联动

如果你在同一 Vault 中维护了其他知识模块（如头脑风暴、研究笔记等），Agent 可以读取这些模块作为分析背景——例如用 你的笔记库 中的投资洞察丰富 P13 市场扫描的上下文。

| 模块类型 | 联动方式 | 典型场景 | 限制 |
|:---|:---|:---|:---|
| **Skills 工具（手脚）** | SOP 步骤按需调用 | 财经搜索/财务数据/选股/宏观数据补充 | 调用失败不阻断主流程 |
| **知识模块（感官增强）** | Agent 读取作为上下文 | 你的投资洞察笔记 → P13 背景 | 只读引用，不修改源模块 |

### 进化边界

| 项目 | 规则 |
|:---|:---|
| P 系列提示词 | **可进化**，但改动需人工审阅后生效 |
| Human in the Loop（P5/P6/P12） | **不可绕过** — 买卖决策必须人工确认 |
| 状态机写入 | **必须通过 SOP 路径** — 不可直接编辑 |
| 报告命名规则（同日覆盖） | **固定** — 系统一致性依赖 |

### 扩展新模块（标准流程）

想要接入新的数据源或分析能力？标准 5 步：

1. **数据脚本** → `Eyes/scripts/fetch_xxx.py`（输出 JSON）
2. **处理提示词** → `Brain/references/xxx.md`（如需要）
3. **SOP 步骤** → 在对应 SOP 中添加可选步骤
4. **环境变量** → 更新 `.env.example`
5. **文档** → 更新 `进阶指南.md` + `README.md`

> Agent 的完整权限表、文件读写规则和模块依赖图详见 `AGENT_GUIDE.md`。

---

## 让 AI 帮你修改 YMOS

YMOS 的设计目标不是让每个用户自己学会写代码，而是让用户知道**应该让 AI 改哪一层**。

| 你想改变什么 | 优先修改 | 同步检查 |
|:---|:---|:---|
| 报告步骤、输入、输出、失败降级 | 对应 `SOP_*.md` | 上下游 SOP、产出路径、成功判定 |
| 投资判断框架、评分与红线 | `Brain/references/` + `rules.json` | P12 与 Human in the Loop 不得被绕过 |
| Reader 显示哪些目录 | `Console/config.json` | 只读白名单与扫描模式 |
| 决策台样式、字段与交互 | `Console/*.html` | `/reader`、`/plan`、`/sop` 路由与 file 模式 |
| 新增外部数据源 | `Eyes/scripts/fetch_xxx.py` | `.env.example`、对应 SOP、失败降级 |
| 新增或替换 Skill | Agent 宿主的 Skills 目录 | `YMOS_SKILL_ROOT`、封装脚本、权限范围 |
| 新增定时任务 | 宿主的任务配置 | 角色文件、SOP、workdir、依赖顺序、成功判定 |
| 新增一个 Agent 角色 | `Agents/*-agent.md` | ORCHESTRATION、PLAYBOOK、读写边界、调度链 |

给 AI 的通用改造提示词：

```text
先读取 YMOS/AGENT_GUIDE.md、YMOS/进阶指南.md 和与本次需求相关的 SOP。
我要实现：<需求>。
先判断应该改 SOP、P 系列、脚本、Skill、Console 还是定时任务；
保持 Human in the Loop、状态写回唯一出口、真实 Key 只放 .env。
修改后检查上下游路径、失败降级、文档和最小验证，不要直接发布或推送。
```

扩展时遵守一个简单规则：**脚本提供原子能力，Skill 提供手脚，SOP 负责编排，Agent 负责角色边界，状态机保存真相。** 不要把五层逻辑全塞进一个定时任务提示词。

---

## 定时任务设置

### 先配三个，而不是一次配十个

推荐新用户先运行两周：

| 优先级 | 任务 | 推荐时段 | 理由 |
|:---|:---|:---|:---|
| 1 | 市场洞察 | 每个交易日早段 | 最低成本的信息压缩 |
| 2 | 投资雷达 | **收盘后** | 把次日决策与开盘执行分开 |
| 3 | 周度复盘 | 周日晚上 | 用账户曲线确定下周攻防基调 |

What’s Hot、策略分析、持仓收口都是按需增强：

- A 股用户装好问财层后，可在收盘数据稳定后增加 What’s Hot。
- 策略分析优先由雷达建议、事件或用户点名触发，不必无标的也每天空跑。
- 持仓收口在策略结论、身份或仓位发生变化后运行，不必为了“任务齐全”机械执行。

核心时序只有一句：**产生决策的任务尽量放在收盘后，开盘只执行已经存在的计划。**

### 一条任务必须写清五件事

```text
1. 什么时候跑     cron 与时区
2. 在哪跑         workdir 固定到 YMOS 根目录
3. 扮演谁         Agents/<role>-agent.md
4. 做什么         对应 SOP 路径与依赖产物
5. 怎么算完成     产出路径 + 结构校验；只写文件，不刷屏
```

示例：

```text
先读取 /你的路径/YMOS/Agents/investment-radar-agent.md，
再读取 Agents/EXECUTION_PLAYBOOK.md 的对应段落，
最后读取 Eyes/SOP_投资雷达.md 并执行。
workdir 固定为 /你的路径/YMOS；只写角色允许的目录；
产物通过 SOP 成功判定后结束，不粘贴报告全文。
```

> 只有定时任务或人真正触发了上述提示词，Agents 才会参与运行。`Agents/` 目录本身不会注册任务，也不会自动启动四个进程。

### 进阶：多角色 Agent 编排

如果你使用支持 subagent / 多角色编排的 Agent 平台，可以进一步把不同 SOP 分配给不同执行体；不支持时由一个主控顺序扮演，仍然遵守相同的读写边界：

- **Market Insight Agent**：只负责看市场，产出市场洞察
- **Investment Radar Agent**：把市场信号翻译成"跟我有关的"桥接报告
- **Strategy Agent**：消费雷达建议，执行策略路由
- **Portfolio State Agent**：统一状态写回，维护持仓/Watchlist 真相源

这种编排让每个角色的职责更清晰、权限更收敛，也更容易调试和迭代。这属于“养系统”的范畴——跑通基础闭环之后，再根据使用习惯逐步优化。

完整的实战时间表、任务取舍和踩坑约束见 [`Agents/SCHEDULES_REFERENCE.md`](Agents/SCHEDULES_REFERENCE.md)。

---

## 问财数据层（可选 · Level 1++）

> **发布状态：Beta。** A 股用户的一条可选增强。V4 首发支持的是 **A 股 What’s Hot 与美股条件选股适配**；
> 公告、研报、新闻、财务脚本已经提供，但仍属于实验性入口，不应理解为“安装一个 Skill 后五项全部自动打通”。

### 它解决什么

Level 0/1/2 解决的是「价格」和「新闻流」。但做 A 股真正缺的是这几样：

| 缺什么 | 靠什么补 | YMOS 里对应的 SOP |
|:---|:---|:---|
| 按条件筛出今天哪些票在动 | 问财选股 | `Eyes/SOP_Whats_Hot.md` |
| 公司自己说了什么（第一手） | 公告搜索 | `Eyes/SOP_问财公告搜索.md` |
| 卖方怎么讲这个故事 | 研报搜索 | `Eyes/SOP_问财经研报搜索.md` |
| 事件的传播势能 | 新闻搜索 | `Eyes/SOP_问财新闻搜索.md` |
| 结构化财务数据（P9 的输入） | 财务查询 | `Eyes/SOP_问财个股财务数据查询.md` |

**信源优先级（重要）**：

```text
公告 / 财报原文  >  结构化财务数据  >  研报叙事  >  新闻事件  >  网页摘要
```

越往右，越是"别人嚼过一遍的"。**做判断时永远先够到左边，够不到再退。**

### What’s Hot 的最小前置条件

运行 `Eyes/SOP_Whats_Hot.md` 的问财三池，需要同时满足：

1. 安装 `hithink-astock-selector` Skill；
2. 在 YMOS 根目录 `.env` 中配置 `IWENCAI_API_KEY`；
3. 如果 Skill 不在自动搜索目录，再配置 `YMOS_SKILL_ROOT`。

其他能力按需增加：

| 能力 | 需要的 Skill / 配置 | V4 发布状态 |
|:---|:---|:---|
| A 股条件选股 / What’s Hot | `hithink-astock-selector` + `IWENCAI_API_KEY` | **Beta 首发适配** |
| 美股条件选股 | `hithink-usstock-selector` + `IWENCAI_API_KEY` | **Beta 首发适配** |
| 港股条件选股 | `hithink-hkstock-selector` + `IWENCAI_API_KEY` | Agent 可直接调用；YMOS 暂无专用链路 |
| 个股财务数据 | `hithink-finance-query` + `IWENCAI_API_KEY` | 实验性，启用前先冒烟测试 |
| 研报搜索 | `report-search` + `IWENCAI_API_KEY` | 实验性，启用前先冒烟测试 |
| 公告 / 新闻搜索 | `IWENCAI_API_KEY`（YMOS 封装脚本直连接口） | 实验性，启用前先冒烟测试 |
| 行情兜底 | `hithink-market-query` + `IWENCAI_API_KEY` | 可选备援，不替代现有价格路由 |

V4 当前验证边界：

- A 股 What’s Hot 与美股热门追踪已经通过 Skill 发现、CLI 入口、输出封装和缺失依赖降级检查。
- 作者当前环境没有可用于本轮测试的 `IWENCAI_API_KEY`，因此发布前仍需用自己的 Key 各跑一次真实最小查询；**Beta 不是“零测试”，也不是“实网全验收”**。
- 财务与研报入口在作者当前环境没有安装对应 Skill；公告与新闻采用直连接口封装。这四项只提供适配骨架，不能与 A 股 What’s Hot 使用同一成熟度口径。
- 港股：SkillHub 当前有官方 `hithink-hkstock-selector`。用户可安装后让 Agent 直接调用；公开版目前没有单独的“港股 What’s Hot” SOP 和结构化封装脚本，因此不把“能选港股”包装成已打通的港股热门自动链。港股价格仍可走现有 Yahoo 路由。

SkillHub 的名称和版本可能升级。表中名称是 YMOS 当前适配器期待的 Skill ID；若官方名称变化，以 SkillHub 当前页面为准，并同步更新 `Eyes/scripts/skill_resolver.py`。

这些外部 Skill **不属于 YMOS**。统一从[同花顺问财官方 SkillHub](https://www.iwencai.com/skillhub)获取；YMOS 不复制、不二次分发官方 Skill。

官方页面的安装流程：

1. 登录 SkillHub；
2. 优先筛选“官方出品”，打开需要的 Skill；
3. 在“安装方式”中选择 **Agent 用户**或 **CLI 用户**；
4. Agent 用户复制官方安装 Prompt 给自己的 AI 助手；CLI 用户按弹窗说明安装；
5. 同一弹窗会提供 `IWENCAI_API_KEY` 获取与配置说明；
6. 官方 Skill 提示升级或旧版报错时，回到 SkillHub 重新下载安装，不在 YMOS 内修补官方 Skill。

### 可以让 Agent 代装，但要给它“官方安装 Prompt”

SkillHub 的 Agent 用户页就是为这条路径设计的：用户登录后，把页面生成的安装 Prompt 复制给自己的 AI 助手。
**YMOS 不能只凭一个 Skill 名称替问财生成下载地址，也不应该让 Agent 猜安装包来源。**

不同 Agent 的默认 Skills 目录不同，所以不能保证“把文件放进任意一个叫 skills 的目录”就能被宿主发现。
把下面这段连同 SkillHub 提供的官方安装 Prompt 一起发给 Agent：

```text
请为当前 YMOS 安装这个问财官方 Skill。

要求：
1. 只执行我随后粘贴的 SkillHub 官方安装 Prompt，不自行猜测下载地址或复制第三方版本。
2. 优先使用你当前宿主的标准 Skills 目录；如果无法确认，就安装到 ~/.config/ymos/skills/。
3. 保留官方 Skill ID 作为文件夹名，不要改写 SKILL.md 或 scripts/cli.py。
4. 安装后确认以下两个文件存在：
   <Skill根>/<官方Skill ID>/SKILL.md
   <Skill根>/<官方Skill ID>/scripts/cli.py
5. 如果安装目录不在 YMOS 自动搜索范围，把其上级目录写入 YMOS 根目录 .env：
   YMOS_SKILL_ROOT=<包含各Skill文件夹的上级目录>
6. 如果只通过 YMOS 封装脚本调用，IWENCAI_API_KEY 由我自己填写到 YMOS/.env。
   如果还要让宿主原生直接调用 Skill，请按官方 Prompt 使用宿主的 Secret / 环境变量机制。
   不要把 Key 写入 Markdown、业务脚本、定时任务文本、命令参数或 Git。
7. 运行 python3 Eyes/scripts/skill_resolver.py 验收。只有显示对应 Skill 为 ✅，
   并且最小查询返回 JSON，才算安装完成。
8. 如果宿主需要刷新 Skills 索引或重启会话，请明确告诉我；不要假装已经注册成功。

下面是 SkillHub 官方安装 Prompt：
[把登录后复制的官方 Prompt 粘贴在这里]
```

这份契约把两件事分开验收：

- **宿主能调用 Skill**：Agent 自己的 Skills 列表里能看到它。
- **YMOS 能调用 CLI**：`skill_resolver.py` 能找到它的 `scripts/cli.py`。

二者并不完全等价。宿主没有注册某个嵌套目录时，YMOS 仍可能直接调用 CLI；反过来，宿主显示
Skill 已安装，但若官方包没有提供 `scripts/cli.py`，当前 YMOS 封装也不能直接消费。

YMOS 建议的目录策略：

| 使用方式 | 推荐位置 |
|:---|:---|
| OpenClaw 等有标准 Skills 目录的宿主 | 使用宿主默认安装目录，不要手动搬家 |
| Codex / 多 Agent 共用的个人 Skills | `~/.agents/skills/`，或使用宿主已经采用的 `~/.codex/skills/` |
| 多个宿主共用同一套 CLI Skills | `~/.config/ymos/skills/` |
| 只为当前 YMOS 临时测试 | `<YMOS根>/skills/`，但不要提交到 Git |

无论装在哪里，`YMOS_SKILL_ROOT` 都应指向**包含各 Skill 文件夹的上级目录**。

安装后不需要改 YMOS 代码，系统会在这些位置自动找：

```text
$YMOS_SKILL_ROOT/            ← 环境变量，优先级最高
~/.openclaw/workspace/skills/
~/.claude/skills/
~/.agents/skills/
~/.codex/skills/
~/.config/ymos/skills/
~/skills/
<YMOS根>/.agents/skills/
<YMOS根>/.claude/skills/
<YMOS根>/.codex/skills/
<YMOS根>/skills/
```

在 YMOS 根目录复制配置样例：

```bash
cp .env.example .env
```

然后编辑 `.env`：

```env
YMOS_SKILL_ROOT=/包含各个Skill文件夹的上级目录
IWENCAI_API_KEY=你的问财服务Key
IWENCAI_BASE_URL=https://openapi.iwencai.com
```

`YMOS_SKILL_ROOT` 不是 Key，它只告诉 YMOS 去哪里找 Skill；`IWENCAI_API_KEY` 才是服务凭据。

- **只走 YMOS 封装脚本**：推荐把 Key 放在 YMOS 根目录 `.env`，封装脚本会传给官方 CLI。
- **还要让 Agent 原生调用 Skill**：按 SkillHub 官方 Prompt 放进宿主的 Secret / 环境变量机制；部分宿主会使用 shell profile。

不要把真实 Key 写进 SOP、报告、业务脚本、定时任务文本、命令参数或 Git，也不要在多个位置无必要地重复保存。

### 检查配置

```bash
python3 Eyes/scripts/skill_resolver.py
```

会逐个列出哪些找到了、哪些没找到、分别在哪。

What’s Hot 最小冒烟测试：

```bash
python3 Eyes/scripts/iwencai_stockpick.py \
  '流通市值大于100亿，今日涨幅大于8%，非ST' \
  --json-out /tmp/ymos_iwencai.json \
  --csv-out /tmp/ymos_iwencai.csv \
  --desc-out /tmp/ymos_iwencai_description.txt \
  --status-out /tmp/ymos_iwencai_status.txt \
  --limit 5 --no-fetch-all
```

确认 status 为成功后，再对 Agent 说 `跑一下 What's Hot`。

### 没装会怎样

**不影响 YMOS 主链路。** 市场洞察、投资雷达、策略分析、P 系列、决策台、
BrainStorm 全都照常跑。只是 What’s Hot 这类依赖问财的 SOP 会留下失败状态并进入
best-effort 降级，同时告诉你缺的是 Skill、路径还是 Key。

### 封装脚本

YMOS 的 SOP 不绑定某个 Agent 宿主或绝对安装路径，而是统一走
`Eyes/scripts/iwencai_*.py` 这层封装。选股、财务和研报脚本调用已安装 Skill 的
`scripts/cli.py`；公告和新闻脚本当前直接调用问财 OpenAPI。这样做是为了
**统一输出格式**（下游 SOP 才能稳定消费）与**统一失败降级**
（Skill 或接口不可用时写状态文件，不能静默失败）。

| 脚本 | 对应能力 | 状态 |
|:---|:---|:---|
| `iwencai_stockpick.py` / `iwencai_us_stockpick.py` | A股 / 美股选股 | Beta 首发适配 |
| `iwencai_a_pullback_scan.py` | A 股核心资产回踩扫描 | 复用 A 股选股适配 |
| `iwencai_announcement_search.py` | 公告 | 实验性直连 |
| `iwencai_news_search.py` | 新闻 | 实验性直连 |
| `iwencai_report_search.py` | 研报 | 实验性 Skill 适配 |
| `iwencai_finance_query.py` | 财务数据 | 实验性 Skill 适配 |
| `skill_resolver.py` | 上面所有脚本共用的 Skill 路径解析器 | 已完成多宿主路径兼容 |

---

## 定时任务的实战配置

参考 [`Agents/SCHEDULES_REFERENCE.md`](Agents/SCHEDULES_REFERENCE.md)——一套真实跑了几个月、迭代过二十多版的配置，含时序闭环设计、A股/美股双链路时间表、以及几条踩坑踩出来的运行约束。

## 推荐 Agent 工具

YMOS 可以在任何支持文件读写和代码执行的 AI Agent 环境中运行。**下表里的工具实际用下来都能跑通 YMOS**——
但要先分清「统筹层」和「执行层」这两个角色，选型才有意义。

### 统筹层：负责长期记忆与协调（建议只选一个）

| 工具 | 特点 |
|:---|:---|
| **Hermes**（推荐）| Nous Research 开源的本地 Agent（[官网](https://hermes-agent.nousresearch.com/) · [GitHub](https://github.com/NousResearch/hermes-agent)）。本地运行、数据不出本机，文件读写 / 代码执行 / 定时任务 / 持久化记忆齐全。**作者目前的 YMOS 就是以 Hermes 作为整体 Agent 框架在跑**——记忆、角色编排、任务串联都收在这一层 |
| OpenClaw（龙虾）| 同样本地优先，支持定时任务与 subagent 编排；云端 Claw 系列原理相同 |

> **为什么只选一个**：YMOS 的价值来自状态机、账户曲线和长期记忆的**连续性**。
> 两个框架各记一半，等于有两份互不知情的记忆，比没有记忆更麻烦。

### 执行层：按单个对话 / 单个任务调用（可以多个并存）

| 工具 | 特点 |
|:---|:---|
| Claude Code / Claude Cowork | 订阅会员产品，文件系统操作和定时任务都成熟 |
| ChatGPT Codex | 支持代码执行和文件操作 |
| 其他任何能读写文件 + 跑 Python 的 Agent | 满足这两点就能执行 SOP |

作者的实际用法：**统筹交给 Hermes，部分定时任务 routing 给 Claude Code 或 Codex 分担**。
执行层跑完把产出写回 YMOS 目录，统筹层照常读文件，互不冲突。

> 执行层随时可以换，因为**真相源始终是磁盘上的 Markdown，不是某个 Agent 的会话上下文**。
> 跑一次深研、改一份 SOP、调一个脚本这类单次任务，挑顺手的用就行。
>
> 不熟悉本地 Agent 框架的用户，只用 Claude 或 ChatGPT 的订阅会员产品也能完整使用 YMOS——
> 只是长期记忆需要你自己更勤快地回写到状态机文件里。

### 模型选择：能用最好的就用最好的

YMOS 的核心价值不在脚本，而在 **SOP + P 系列提示词的分析质量**——这完全取决于底层模型的能力。模型强 1%，投资判断就可能好 1%，而在投资里，1% 的差距可以是决定性的。

模型名称和版本更新很快，文档不再固定写某个“当前最强”型号。请在你的 Agent 宿主中选择当时可用、推理与长上下文能力最强的稳定模型。

不要因为省 token 而降级模型。对投资来说，模型表现好 1%，对胜率的提高都不一样——视角的补充更全面，策略的制定更严谨，风险的识别更敏锐。省下来的 token 成本，远不如一次误判的代价。

如果资源有限，**优先把最好的模型用在策略分析环节**（Brain 模块的 P5/P6/P8/P9/P12 等），这里的判断质量对最终投资决策影响最大。市场洞察和投资雷达可以用稍弱的模型，但策略分析和纪律审查一定要用最强的。

---

## 系统扩展性设计理念

YMOS 提供的是：

1. **开箱即用的最小闭环**：纯 RSS + Yahoo Finance，零配置
2. **完整的投资思路框架**：P1-P18 覆盖从建档到复盘的全流程
3. **持仓/Watchlist 管理框架**：状态机 + 个股文件夹 + 自动化 P 链
4. **可魔改的扩展接口**：
   - 数据层（Eyes/scripts/）：增加任何 API 数据源（价格/新闻/基本面）
   - 处理层（Brain/references/）：自定义 CIO 提示词、修改 P 系列提示词
   - 路由层（各 SOP）：增加新的暗号和工作流
   - 展示层：所有产出物是 Markdown，可接入任何展示工具

### 可视化看板（Dashboard）

持仓收口 SOP 的最后一步会自动生成一个可视化 Dashboard（HTML 文件），综合展示当日的市场洞察、投资雷达、持仓状态和待办事项。浏览器打开即可查看，无需额外工具。

**输出路径**：`持仓与关注/dashboard/YYYY-MM/dashboard_YYYY-MM-DD.html`（按月归档，每日一个文件）

Dashboard 由 Agent 根据 SOP 中的布局要求直接生成（软路由），不依赖固定的脚本解析——即使你修改了报告格式，Agent 也能正确理解内容并渲染看板。

如果你希望进一步定制看板样式或集成到自己的前端系统，可以基于生成的 HTML 做二次开发。所有数据来源都是本地 Markdown 文件，没有外部依赖

5. **策略进化的核心载体**：P1-P18 提示词是框架样板——它代表一种经过验证的投资思路和决策路由。
   真正的进阶不只是接更多数据源，而是**结合你自己的投资策略，改造 P 系列提示词和 SOP 路由**。
   这是系统大脑进化的核心。

**你的系统，你做主。** 框架搭好了，能力升级是渐进的——技术配置让手脚更灵活，策略进化让大脑更聪明。

---

## 数据层级对比

| 层级 | 数据源 | CIO 处理 | Finnhub News | P13 输入质量 | 成本 |
|:---|:---|:---|:---|:---|:---|
| **基础**（开箱即用） | 15 个免费 RSS | 需要 | 无 | 良好 | 免费 |
| **+Finnhub** | RSS + Finnhub | RSS 部分需要 | 有 | 更好 | 免费（注册） |
| **+Market API** | 结构化 API | 不需要 | 可选 | 最佳 | 按需联系 |

---

## 文档协同速查

| 文档 | 面向谁 | 核心内容 |
|:---|:---|:---|
| `README.md` | 新用户 | 架构总览、目录结构、暗号速查、数据配置概览 |
| `AGENT_GUIDE.md` | Agent | 读取优先级、文件权限表、模块依赖、写回规则 |
| `进阶指南.md`（本文件） | 进阶用户 | 数据源升级、策略定制、Agent 扩展、系统扩展理念 |
| `Agents/README.md` | Agent + 进阶用户 | 四角色协议如何真正接入手动任务或定时任务 |
| `UPGRADE.md` | V3 用户 | 保留个人数据并迁移到 V4 |
| `CHANGELOG.md` | 所有人 | 每个版本改了什么 |
| `总入口暗号.md` | Agent + 用户 | 暗号路由表 |

---

*文档版本：2026-07-27 · YMOS V4 · 三模块 + 运行时 + 决策台*
