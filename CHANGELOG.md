# 更新日志

本文件是 YMOS 版本历史的**唯一真相源**。

---

## V4.0 — 2026-07-27

> **一句话**：V3 是一套能跑的系统；V4 是一个**能装下你自己策略的框架**。

V3 之后的四个月，这套系统在真实市场里持续跑、也持续付学费。V4 把这段时间长出来的两层能力
补进公开版，并把「框架」和「某个人的策略」这件事讲清楚。

### 新增：Console 可视层

Reader 与两个把纪律变成**物理闸门**的工具，共用一个本地服务和一个端口。

- **交易计划台** — 收盘后定调、审持仓、审观察、列明日动作。第二天开盘只执行，不在盘中现想。
- **新高买卖决策台** — 开新仓 / 加仓 / 止盈 / 止损各有一组门，全绿才允许扣扳机，漏一条就拦下来。
- **Reader** — 把散在目录里的报告聚成一页，负责阅读，不承担定时任务管理。
- **决策留痕** — 每一次扣扳机、每一次被拦截，都写回你 vault 的 `决策审计/`。
- 自带精简后端（`Console/server.py`，stdlib 零依赖），数据全部落成你自己的 Markdown。
  不启服务也能用，退化为本地暂存 + 导出（.md / 剪贴板 / JSON / 打印）。
- 规则外置：`rules.example.json` 里是**一份样例**，复制成 `rules.json` 就能换成你自己的门禁与仓位口径。

### 新增：Agents 运行时编排层

把「一堆 SOP」升级成「4 个角色 + 一条调度链」。

- 四个角色：Market Insight / Investment Radar / Strategy / Portfolio State。
- 两条硬规矩：**顺序不能乱**（洞察 → 雷达 → 策略 → 状态写回）、**状态写回只有一个出口**。
- 含每个角色的五段式执行手册、异常处理、定时任务编排。
- 宿主无关：Claude Code / Codex / OpenClaw 等都能用；单主控扮演 4 角色即可，
  等宿主支持真子 agent，协议直接映射，文档不用改。

### Reader 报告浏览器

把散在十几个目录里的报告聚合成一个浏览器页面。**只读、本地后端零第三方依赖、只绑本机。**

- **config.json = 目录对照表**：整块目录配置外置成 JSON，示例已按 V4 结构配好六个分区。
  想改 Reader 显示什么，改配置，不用动代码。
- 七种扫描模式（tree / flat-md / flat-whitelist / tree-text / flat-text /
  latest-month-dirs-text / recent-days-text）。
- 支持接入 YMOS 之外的目录（`roots` 里加一条，支持绝对路径与 `~`）。
- 「📅 今日全家桶」一键载入当天所有日报；`⌘K` 过滤；右键复制路径 / 打开位置 / Obsidian。
- 跨平台：文件管理器与剪贴板在 macOS / Windows / Linux 分别走各自命令（原版只支持 macOS）。
- 已合并进 `Console/`，与两个决策台共用默认端口 5273，只需启动一个服务。

### 新增：BrainStorm 思考沉淀层

接住那些**没法结构化、但决定长期水位**的东西——复盘时的感悟、想不通的困惑、
反复出现的同一个错误。

- **三层架构**：原始输入（Raw_Thoughts）→ AI 整理（Summarized_Insights）→ 长期归档
  （MEMORY / 投资感悟归档 / 状态机）。按日保存，按月归档。
- **投资感悟归档的三个抽屉**：短期灵感 → 长期框架 → 执行清单，按"同类感悟出现 ≥3 次"升级。
- **和投研链路双向联动**：决策留痕/周度复盘/P11 复盘 → 感悟 → 升级为原则 →
  **回写 `当前关注方向与投资偏好.md`** → P5/P12 在下次出手时用它拦你。
  这个闭环是 BrainStorm 存在的意义：感悟不回写，就永远不改变行为。
- `scripts/brainstorm_memory.py` 上下文加载器（零依赖）。
  修复了两个既有问题：Insight 目录路径与实际约定不符、写入未按月归档。

### 新增：问财数据适配层（可选 · Beta）

A 股用户的结构化数据增强。V4 首发适配 A 股 What’s Hot 与美股条件选股；
公告 / 研报 / 新闻 / 财务四个入口随仓库提供，但标记为实验性，启用前需用用户自己的 Key 冒烟测试。

- `Eyes/SOP_Whats_Hot.md`、`SOP_美股热门追踪.md`、`SOP_Watchlist自动建档.md`、
  `SOP_核心资产动态判定表.md`、`SOP_A股核心资产回踩扫描.md` 及问财四件套 SOP。
- `Eyes/scripts/skill_resolver.py` — **统一的 Skill 路径解析器**：
  支持 `YMOS_SKILL_ROOT` 环境变量 + 五个常见位置自动查找；
  Skill 没装时给出安装指引而不是抛异常，**不影响主链路运行**。
- 信源优先级原则：公告/财报原文 > 结构化财务 > 研报叙事 > 新闻事件 > 网页摘要。
- 相关封装脚本 8 个（`iwencai_*.py`、`mx_stockpick.py`）。
- 官方问财 Skill 不随 YMOS 二次分发；用户从 SkillHub 安装，YMOS 负责跨宿主发现、输出归一化和失败降级。

### 新增：定时任务实战参考

`Agents/SCHEDULES_REFERENCE.md` —— 一套真实跑了几个月、迭代过二十多版的配置。

- **时序闭环**：周日周度基调 → 每日收盘后雷达定次日调 → 次日策略分析 → 开盘只执行。
  核心是把**产生决策的任务全部挪到收盘后**。
- A 股 / 美股双链路时间表（含美股链"周二到周六"这个容易配错的 off-by-one）。
- 五条踩坑约束：只写文件不回推、收窄工具集、固定 workdir、任务名带前缀、产出做结构校验。
- **什么该停掉**的判据，以及「先只配三个」的起步建议。

### 新增：升级指南

`UPGRADE.md` —— V3 用户怎么升到 V4（git 路径 / ZIP 路径 / 哪四个文件绝对不能覆盖）。

### 新增：纪律层

- `持仓与关注/SOP_周度复盘.md` — 用**账户资金曲线**定下周攻防基调，含连续 N 周不盈利的熔断规则。
- `持仓与关注/账户资金曲线.md` — 账户级状态的真相源（模板）。单票纪律管不住账户级风险。
- `持仓与关注/击球区注册表.md` — 把买点从「盘中的感觉」变成「盘前定好的条件」（模板）。
- `持仓与关注/主线首发信号跟踪.md` — 板块级首发信号与代表权判断（模板）。
- `Brain/references/p17-core-asset.md` — 核心资产判定七问（含股性 / 可持有性、Raise-and-Beat 加权）。
- `Brain/references/p18-daily-mover-scanner.md` — 每日异动 × 核心资产扫描。
- `Brain/SOP_单标的P9P17专项调研.md` — 人工点名标的的深研入口。

### 修复

- 修复问财选股、财务和研报封装脚本缺少 `skill_resolver` 导入、Skill 未安装时可能对 `None` 调用 `.exists()` 的问题。
- 所有问财路径统一读取根目录 `.env`；`.env.example` 补齐 `YMOS_SKILL_ROOT`、`IWENCAI_API_KEY`、`IWENCAI_BASE_URL`。
- 明确问财 Skill 只从官方 SkillHub 安装，推荐使用宿主默认 Skills 目录；项目内临时 `skills/` 已加入 `.gitignore`。
- 公告 / 新闻封装从第三方 `requests` 改为 Python 标准库 `urllib`，避免可选数据层额外引入运行依赖。
- 澄清 `Agents/` 是角色与调度协议，不会仅因复制文件而自动运行；定时任务需显式读取角色卡、Playbook 与 SOP。
- 统一进阶指南与实战调度口径：先配市场洞察、收盘后投资雷达、周度复盘，避免盘中临时制造决策。
- `p4-radar.md` 标题笔误（写成了 `# P2`）、`p16-earnings.md` 缺标题行。
- `.gitignore` 里 `2026-*` 的硬编码年份改为通配，2027 年的报告不会被误提交。
- `.gitignore` 补上 `持仓与关注/dashboard/`、`Brain/交易计划/`、`Brain/决策审计/`。
- 补齐 `SOP_持仓收口.md` 缺失的版本页脚；统一全仓页脚版本行。
- 脚本里过期的版本串（`YMOS-V2` 配置注释、`YMOS/2.0` User-Agent）。
- 移除遗留的空目录 `obsidian投资知识库/`（无任何文档引用，已被个股文件夹取代）。

---

## V3.1 — 2026-03-28

新增投资策略诊断模块 `Brain/ymos-diagnosis/`：问诊（5 层消解漏斗）与体检（7 项检验）双模式。
独立于主流程，不依赖状态机，不写任何文件。

---

## V3.0 — 2026-03-19

首个完整公开版本。

- 三模块架构：Eyes（看市场）→ Brain（做分析）→ 持仓与关注（管状态）
- 5 条策略路由、P1–P16 提示词资产
- 三源价格路由（Finnhub → Tushare → Yahoo 自动分流）
- P12 纪律审查作为建议到达用户前的最后一道关卡
- 轻脚本 + 重 SOP + 软路由；Markdown-first；Human in the Loop

---

## V2.x — 内部迭代

架构探索期，未公开发布。

---

## V1.0 — 2026-02

早期投研中台版本，已归档至 `v1-legacy` 分支。
