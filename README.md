# YMOS V4 — 勇麦投资操作系统

**Yongmai Operating System** · 把你自己的投资思想，变成一套能运行、能审计、能进化的系统

[![Version](https://img.shields.io/badge/version-4.0.0-blue.svg)](https://github.com/Evan-XYZ/YMOS/releases)
[![License: CC BY-NC 4.0](https://img.shields.io/badge/license-CC%20BY--NC%204.0-lightgrey.svg)](LICENSE)

> 以市场触发为起点、以提示词软路由为中枢、以状态机与个股知识库为记忆层、以 Human 最终决策为落点的人机协作投资操作系统。

> ⚠️ **它不预测行情，也不告诉你买什么。** 行情不可预测，只能应对。
> YMOS 解决的是另一件事：**把你自己的判断流程固定下来，让你在最容易犯错的时候，还能按自己定的规矩走完。**
> 这里没有任何投资建议。二级市场九死一生，盈亏自负。

> **项目定位**：投资是这套方法的压力测试场，不是信号产品。YMOS 开源的是“如何把判断做成可审计系统”的架构、SOP、提示词和样板；任何热门榜、观察池或报告都只是研究输入，不是荐股、代客决策或收益承诺。

> 🔄 **V4 的关键变化**：把「框架」和「某个人的策略」掰开了。
> 框架给所有人，策略只是一份可替换的样例。详见 [CHANGELOG](CHANGELOG.md)。

---

## YMOS 解决什么问题

独立投资者面对的核心矛盾：**信息太多、处理不过来、动作变形**。

每天几十条财经新闻、持仓的涨跌、Watchlist 的异动、宏观政策的变化……大部分时间花在了「收集 → 整理 → 焦虑 → 冲动操作」的循环里。真正该做的事——**冷静地审视策略、验证逻辑、做纪律性的决策**——反而被挤到了边角。

YMOS 帮你系统性地捕捉两类投资机会：

| 机会类型 | 特征 | YMOS 怎么帮你 |
|:---|:---|:---|
| **长线：宏观 + 公司基本面** | 宏观周期拐点、危机定价、持仓公司的财报/商业模式/护城河变化等重大事件 | Eyes 持续监控宏观信号 + 事件异动，P13 识别战略级信号；投资雷达跟踪持仓标的动态，个股知识库积累上下文；Brain 通过 P2/P8/P9 评估阶段与定价 |
| **短线：势能 + 事件驱动** | 美股财报→A股映射、板块轮动、事件传导的短期价差 | Eyes 实时追踪价格异动 + 关联传导，投资雷达自动匹配你的板块关联逻辑，策略分析给出具体路由建议 |

---

## 为什么做 YMOS

AI 的能力已经强到可以做一些不一样的事了。

我做 YMOS，首先是**解决自己的问题**——理清自己的投资思路，把重复性的信息收集、整理、监控交给 AI，让 AI 帮我补充视角、覆盖盲区，而我自己专注于最关键的深度调研和最终决策。这个过程本身就是一次思路梳理：你必须把"我到底怎么做投资"想清楚、写下来，AI 才能帮得上忙。

其次，是**给同样在摸索的人提供一个参考**。很多独立投资者都在想：AI 这么强，到底怎么用在投资上？怎么搭一个靠谱的人机协作框架？YMOS 不是标准答案，但它是一个**经过实盘验证的、可以直接拿来用的起点**。你可以在这个框架上按自己的风格改造，而不用从零开始摸索。

**把重复劳动交给 AI，把你解放出来，专注于打磨策略和做关键判断。**

---

## V4 这次真正新增了什么

V3 解决了“让 AI 按一套 SOP 做投研”；V4 继续向前，把**研究、执行、复盘和个人认知进化**接成一个闭环。

| V4 核心更新 | 它解决的问题 | 入口 |
|:---|:---|:---|
| **P17 核心资产判定七问** | 不再只问“公司好不好”，而是同时审计核心性、应得价值、份额捕获、稀缺性、终局空间、势能阶段和股性/可持有性 | [`p17-core-asset.md`](Brain/references/p17-core-asset.md) |
| **单标的 P9 + P17 专项调研** | Human 临时发现一个标的时，不必直接建仓或塞进正式 Watchlist；先补证据链、估值和核心资产判定，再决定是否进入真实观察 | [`SOP_单标的P9P17专项调研.md`](Brain/SOP_单标的P9P17专项调研.md) |
| **问财数据适配层（Beta）** | 首发适配 A 股 What’s Hot 与美股条件选股；财务、公告、研报、新闻保留为实验性入口。官方 Skill 与 YMOS 主项目解耦，未安装也不阻塞主链路 | [`进阶指南.md`](进阶指南.md#问财数据层可选--level-1) |
| **Console + Reader** | 一个端口聚合报告、制定交易计划、执行买卖门禁并留下审计记录 | [`Console/README.md`](Console/README.md) |
| **BrainStorm 工作区** | 把交易感悟从口头想法变成 Insight、长期原则和执行清单，并回写到下次真正会被 P5/P12 读取的位置 | [`BrainStorm/README.md`](BrainStorm/README.md) |
| **Agents + 账户级纪律** | 用角色协议固定执行顺序，用账户曲线、周度复盘、熔断和击球区约束单票纪律覆盖不到的风险 | [`Agents/README.md`](Agents/README.md) |

### P17 七问：从“看起来不错”到“是否值得长期跟踪”

P17 是 V4 最重要的投资判断升级之一。它要求 Agent 正面回答：

1. **是不是核心资产**：它在产业链里是否不可替代，失去它会不会真正影响行业？
2. **内在价值是多少**：对标谁、应该值多少、当前价格与应得价值之间有多大 gap？
3. **能捕获什么份额**：利润池份额是否上升，增长根因能否持续？
4. **有没有稀缺性溢价**：唯一供应、寡头生态位、客户锁定或工作流嵌入是否值得溢价？
5. **终局空间多大**：从 3–5 年 TAM、收入利润和份额捕获看，市场是否已经 price in？
6. **势能传播到哪一段**：萌芽、加速、成熟还是失效？业绩是否持续 Raise-and-Beat？
7. **股性是否适合持有**：相对强度、回撤修复、趋势流畅度、量价与事件反馈是否支持长期跟踪？

七问最后统一收敛成：

```text
核心资产 → 应得价值 → 当前势能/业绩兑现 → 股性/资金认可 → 执行动作建议
```

这里必须区分三件事：**A 类资产不等于今天可以买，长期空间不等于当前赔率，研究结论不等于交易指令。**
真实买入、加仓或卖出仍要经过 P5/P12 与 Human 最终确认。

> P17、P9/P17 专项调研和页面门禁都来自作者自用系统的真实迭代，是一份可运行的默认样板，不是适合所有人的标准答案。你应根据自己的市场、周期、能力圈和风险偏好修改它们。

完整版本变化见 [CHANGELOG.md](CHANGELOG.md)。

---

## 核心设计理念

### 轻脚本 + 重 SOP + 软路由

这不是一个传统的量化系统，也不是一个仪表盘工具。YMOS 的核心不是代码，而是**投资思路的结构化表达**：

- **脚本层**：只做原子能力——取数据、提取信息、落盘存储。轻量、零依赖、开箱即用
- **编排层**：通过自然语言暗号（如「跑一下投资雷达」）和 Markdown SOP 驱动整个工作流。不需要写代码，说人话就能触发
- **判断层**：由大模型 + P 系列提示词完成复杂决策。P1-P18 覆盖从建档到复盘的全流程，每一个 P 模块都是一个经过验证的投资思维框架

### 为什么不做成全自动？

因为投资的最终决策权必须在人手上。YMOS 坚持 **Human in the Loop**：AI 负责收集、分析、给出路由建议（「建议跑一下 `我想买 TSLA`」），但**永远不替你按下买入键**。系统的纪律审查模块（P12 Referee）是最后一道关卡，通过了也只是建议，决策权始终是你的。

### Markdown-First：你的数据，你做主

所有产出物——报告、状态机、个股知识库、策略日志——全是 Markdown 文件。没有私有格式、没有数据库锁定、没有 SaaS 依赖。放在本地任意目录都能运行，搭配 Obsidian 等 Markdown 笔记工具使用体验更佳。

---

## V4 架构：三模块 + 运行时 + Console 可视层

```
        ┌───────────────────────────────────────────┐
        │        Agents/ (运行时编排层)              │
        │   4 个角色 + 一条调度链，管顺序和边界       │
        └───────────────────┬───────────────────────┘
                            │ 编排
┌───────────────────────────▼───────────────────────┐
│                    Eyes/ (眼睛)                    │
│         盯市场：趋势 + 价格 + 信号                 │
│         市场洞察 → 投资雷达（桥接报告）             │
└───────────────────────────┬───────────────────────┘
                            │ 下一步建议（暗号路由）
┌───────────────────────────▼───────────────────────┐
│                   Brain/ (大脑)                    │
│         做分析：策略路由 + P 链执行                │
│         策略分析 + 初始调研 + P1-P18               │
└───────────────────────────┬───────────────────────┘
                            │ 写回状态
┌───────────────────────────▼───────────────────────┐
│                持仓与关注/ (状态层)                │
│         管状态：状态机 + 个股文件夹                │
│         标的管理 + 策略偏好 + 账户曲线             │
└───────────────────────────┬───────────────────────┘
                            │ 我到底该不该动手？
        ┌───────────────────▼───────────────────────┐
        │     Console/ (Reader + 决策台)             │
        │   聚合报告；把纪律变成闸门并让决策留痕       │
        └───────────────────┬───────────────────────┘
                            │ 这次为什么又犯了同样的错？
        ┌───────────────────▼───────────────────────┐
        │      BrainStorm/ (思考沉淀)                │
        │   Raw → Insight → 原则，回写投资偏好        │
        └───────────────────────────────────────────┘
```

**Eyes 看到什么 → Brain 分析怎么办 → 持仓与关注记住结论。** 三个模块各司其职，信息单向流动，不互相污染。

投资雷达是整个系统的**桥接报告**——它回答的核心问题是「市场发生的事跟我有什么关系？下一步该做什么？」，然后把结论以暗号的形式传递给 Brain。

V4 在这三层之外，上下各加了一层：

| 层 | 解决什么 | 为什么 V3 不够 |
|:---|:---|:---|
| **[Agents/](Agents/README.md)** 运行时 | 定角色、定顺序、定边界 | V3 全靠暗号触发，职责混在一起，顺序靠自觉——行情一急就容易跳步 |
| **[Console/](Console/README.md)** Reader + 决策台 | 把报告聚成一页，并把纪律变成流程闸门 | 系统产出越多越难翻；系统能给结论，也拦不住你盘中凭感觉动手 |
| **[BrainStorm/](BrainStorm/README.md)** 思考沉淀 | 接住那些没法结构化、但决定长期水位的想法 | 前面几层都在处理数据；但**如果你今年犯的错和去年是同一个，缺的不是方法，是这一层** |

---

## 用户日常只看四份报告

| 报告 | 回答的问题 | 路径 |
|:---|:---|:---|
| **市场洞察** | 今天市场发生了什么？ | `Eyes/市场洞察/` |
| **投资雷达** | 和我有什么关系？下一步做什么？ | `Eyes/投资雷达/` |
| **策略分析日志** | 分析了什么？待我确认什么？ | `Brain/策略分析/` |
| **持仓备忘录视图** | 当前持仓的数据状态快照 | `持仓与关注/持仓备忘录_视图.md` |

你不需要每天打开十个网站、刷三个 App。收盘后说四句话，系统帮你把信息压缩成四份产出，你审阅、确认、决策。

> 说明：**持仓备忘录视图是「数据状态」，不是「看板」。** 它由持仓收口自动生成，用于校验状态机是否一致，日常不需要盯着看。
> 真正每天要用的是 **[Console](Console/README.md)**：Reader 翻报告，两个决策台做计划、过门禁。

---

## 上手指南

> 💡 **不确定自己的投资策略？** 可以先用内置的 [投资策略诊断模块](Brain/ymos-diagnosis/) 做一次体检，梳理清楚你的投资思路，再开始使用 YMOS。让 AI 读取 `Brain/ymos-diagnosis/SKILL.md`，说「帮我做一次投资诊断」即可。

### Step 0：说「开始使用」，AI 带你走一遍

第一次使用？把 YMOS 根目录和总入口暗号的路径给 AI，然后说「开始使用」：

```
读一下这个文件：/你的本地路径/YMOS/总入口暗号.md

开始使用
```

> **为什么要先给路径？** AI 需要知道你的 YMOS 装在哪里，才能正确读写文件。给了总入口暗号的路径后，AI 会自动推导出整个项目的目录结构。后续对话中直接说暗号即可，不需要每次都给路径。

AI 会启动入职引导，一步步跟你对话完成：

1. **填写投资偏好**（最重要）—— AI 会分 9 个维度访谈你，然后帮你写入 `当前关注方向与投资偏好.md`
2. **添加你的持仓** —— 告诉 AI 你目前持有什么，它帮你初始化持仓状态机
3. **添加你的关注** —— 告诉 AI 你在观察什么，它帮你建立 Watchlist
4. **引导你跑第一轮** —— 体验一次 `跑一下市场洞察` → `跑一下投资雷达` 的完整闭环

> 整个过程大约 10-15 分钟。完成后你的系统就是个性化的了。

#### 为什么投资偏好这么重要？

`持仓与关注/当前关注方向与投资偏好.md` 是整个系统的灵魂文件：

| 维度 | 为什么重要 |
|:---|:---|
| **投资者画像** | 告诉系统你的风险承受度和心理弱点，P5/P12 会据此拦截不合理的操作 |
| **仓位配置框架** | 定义 PVE/PVP/现金的目标占比，系统据此判断你是不是"超配"了 |
| **板块关联逻辑** | 告诉系统哪些市场信号跟你有关，投资雷达据此过滤噪音 |
| **核心心法与历史教训** | 你用真金白银换来的经验，系统会在关键时刻提醒你 |

填得越详细，系统越懂你。每次投资策略有重大调整时，回来更新这个文件——它不是写一次就完事的，而是跟你的策略一起进化。

### Step 1：零配置跑通最小闭环

YMOS 开箱即用，不需要任何 API Key：

```
跑一下市场洞察          → RSS 抓取 + P13 分析 → 市场日报
跑一下投资雷达          → Yahoo Finance 价格 + 趋势回顾 → 桥接报告
```

看到 `Eyes/` 下生成了报告文件？恭喜，最小闭环已跑通。

### Step 2：管理你的标的

```
关注 TSLA               → 加入 Watchlist，可选触发初始调研
建仓 NIO                → 从 Watchlist 转入持仓
调研一下 NVDA           → AI 自主完成 P1 建档 + P4 雷达 + P2 阶段判断
深研一下 NVDA           → P9 估值 + P17 七问专项调研，默认先进入潜在观察
```

每个标的在 `持仓与关注/` 下有独立文件夹，里面的所有文件（知识库、备忘录、历史策略报告、你手动拖入的研报）都会成为 AI 分析时的上下文。

`深研一下 [ticker]` 特别适合“我刚看到一个公司，但还不确定值不值得正式跟踪”的场景。它默认先落到潜在观察层，不因为用户点名就自动升级 Watchlist，更不会直接给出买入指令。

### Step 3：策略分析

```
我想买 TSLA             → P2 → P9 → P17 → P5 FOMO 审计 → P12 纪律审查 → 你决策
持有怎么看 NIO          → P2 → P6 利润守门员 → P12 → 你决策
跑一下策略分析          → 自动读取雷达建议，批量处理
```

每条策略路由都经过 P12 纪律审查才能到你面前。系统会告诉你「P12 通过/不通过」和理由，但最终按不按按钮是你的事。

### Step 4：按需升级数据源

| 级别 | 配置 | 解锁能力 |
|:---|:---|:---|
| Level 0（零配置） | 无需 API Key | RSS + Yahoo Finance |
| Level 1 | `FINNHUB_API_KEY`（免费注册） | +美股/Crypto 实时报价和新闻 |
| Level 1+ | `TUSHARE_TOKEN`（免费注册） | +A股实时行情 |
| Level 1++（可选 · Beta） | 外部问财 Skills + `IWENCAI_API_KEY` | 首发支持 A 股 What’s Hot / 美股条件选股；公告、研报、新闻、财务为实验性入口 |
| Level 2 | `YMOS_MARKET_API_*` | +结构化市场事件 API |

将 `.env.example` 复制为 `.env`，再把需要的 Key 和 Skill 路径写进去。`.env` 已被 Git 忽略，不要把真实凭据写进 SOP、脚本或提交记录。

> 问财不是 YMOS 内置能力：需要先安装对应 Skill，再配置服务 Key。完整步骤与自检命令见 [`进阶指南.md`](进阶指南.md#问财数据层可选--level-1)。

### Step 5：打开 Console（V4 新增，强烈建议）

前四步解决的是「信息怎么进来、怎么分析、状态记在哪」。
Step 5 解决最后一厘米：**你知道该怎么做之后，怎么保证自己真的照做。**

```bash
cd Console
cp config.example.json config.json     # 编辑它，指向你自己的 vault
python3 server.py
```

浏览器打开 <http://localhost:5273>，你会看到三个页面：

- **Reader** — 把散在目录里的报告按你的使用习惯重新分组；只读、不改文件。
- **交易计划台** — 收盘后定调、审持仓、审观察、列明日动作。第二天开盘只执行，不在盘中现想。
- **新高买卖决策台** — 开新仓 / 加仓 / 止盈 / 止损各有一组门，**全绿才允许扣扳机**，漏一条就拦下来。
  每一次扣扳机、每一次被拦，都会写回你 vault 的 `决策审计/`。

> 本地后端**零第三方依赖**，不用装任何 pip 包。不启服务也能直接双击两个决策台使用，只是数据不写回 vault；
> Reader 需要本地服务读取目录，并默认通过 CDN 加载 Markdown 渲染与安全过滤库。
>
> ⚠️ **里面的门禁条目和仓位口径是一份样例，不是标准答案。**
> 复制 `rules.example.json` 为 `rules.json` 改成你自己的——这正是 V4 的核心主张：
> **框架是共用的，策略是你自己的。**
>
> Reader 的目录分组在同一个 `config.json` 里修改。三个页面共用一个服务、一个端口，不需要再单独启动 Reader。

详见 [`Console/README.md`](Console/README.md)。

### Step 6：让 BrainStorm 把感悟变成下一次会生效的规则

BrainStorm 不是资讯收集区，也不是为了多写一份日报。它处理的是那些暂时无法量化、却会反复影响投资行为的内容：

```text
开始今天的头脑风暴      → 原样记录今天的想法、困惑和交易理由
整理一下今天说的        → 压缩成 Insight、行动项和待验证问题
复盘一下这段时间        → 从近期 Insight 里寻找反复出现的模式
```

沉淀顺序是：

```text
Raw Thought → Summarized Insight → 短期灵感 → 长期框架 → 执行清单
                                                        ↓
                           当前关注方向与投资偏好 / 击球区 / 红线
                                                        ↓
                                      P5 / P12 在下一次出手时调用
```

不是每条想法都要升级。只有反复出现、经验证值得执行的认知，才写回长期规则。详见 [`BrainStorm/README.md`](BrainStorm/README.md)。

---

## 暗号速查

### 每日运行
```
跑一下市场洞察          → 市场日报
跑一下投资雷达          → 桥接报告（趋势+价格+建议）
跑一下策略分析          → 处理雷达建议
```

### 手动触发
```
关注 [ticker]           → 新增 Watchlist
建仓 [ticker]           → Watchlist → 持仓
清仓 [ticker]           → 持仓 → Watchlist
调研一下 [ticker]       → P1+P4+P2+P9 深度调研
我想买 [ticker]         → 买入策略路由
我想卖 [ticker]         → 卖出策略路由
持有怎么看 [ticker]     → 持有评估
做个仓位再平衡          → 组合再平衡
查一下价格              → 价格扫描
深研一下 [ticker]       → P9+P17 专项调研
跑一下周度复盘          → 账户曲线定下周基调 + 红线自检 + 熔断判定
```

### 投资诊断
```
诊断一下我的策略        → 投资策略诊断（问诊 / 体检）
帮我看看我的投资        → 同上
```

---

## 定时任务（可选）

不要一上来把所有 SOP 都设成定时任务。建议先启用三个稳定任务：

| 任务 | 建议触发 | 作用 |
|:---|:---|:---|
| 市场洞察 | 你关注市场收盘后，或每天一次 | 建立市场背景与事件地图 |
| 投资雷达 | 市场洞察完成后 | 结合持仓、关注与价格变化筛选待处理事项 |
| 周度复盘 | 每周一次 | 校正偏好、状态、策略执行和系统问题 |

What's Hot 可在 A 股收盘数据稳定后按需增加；策略分析由雷达建议或用户主动触发；持仓收口只在实际操作或事实变化后运行。

定时任务提示词至少要明确**角色、SOP、工作目录、成功标准**：

```
你现在承担 Market Insight Agent 角色。
工作目录：/你的路径/YMOS
读取：Agents/Market_Insight_Agent.md
执行：Eyes/SOP_市场洞察.md
成功标准：生成当日市场洞察；失败必须返回具体步骤和错误，不静默跳过。
```

> Agent 文件和 SOP 文件本身不会自动运行；实际触发器是你的 Agent 主控、定时任务或手动指令。完整模板见 [Agents/SCHEDULES_REFERENCE.md](Agents/SCHEDULES_REFERENCE.md)。
> 💡 **关键提示**：设置定时任务时务必带上 `总入口暗号.md` 的完整路径。Agent 需要通过这个文件定位你的 YMOS 目录和所有 SOP。没有记忆功能的 Agent 每次执行都需要这个路径作为入口。

> 详细的定时任务配置和执行时间估算见 [`进阶指南.md`](进阶指南.md)。

---

## 人机边界

| 环节 | AI 做 | Human 做 |
|:---|:---|:---|
| 数据收集 | 脚本自动取数 | — |
| 市场识别 | P13 分析 | 审阅洞察 |
| 持仓追踪 | 价格扫描 + 雷达 | 关注异常 |
| 个股研究 | P1+P4+P2 建档 | **深度调研**：读研报、挖行业、理解公司，把材料拖入个股文件夹 |
| 策略建议 | P 链路执行 | **最终决策** |
| 交易执行 | — | **直接操作** |

---

## 目录结构

```
YMOS/
├── Eyes/                          # 眼睛模块
│   ├── SOP_市场洞察.md            # P13 市场日报
│   ├── SOP_投资雷达.md            # 桥接报告（核心）
│   ├── scripts/                   # 取数脚本（零第三方依赖）
│   ├── 市场洞察/                  # 按月归档：YYYY-MM/ + Raw_Data/
│   └── 投资雷达/                  # 按月归档：YYYY-MM/ + Raw_Data/
│
├── Brain/                         # 大脑模块
│   ├── SOP_策略分析.md            # 策略路由（5条路线）
│   ├── SOP_初始调研.md            # P1+P4+P2 建档
│   ├── SOP_单标的P9P17专项调研.md # 人工点名标的的深研入口
│   ├── references/                # P1-P18 提示词资产（核心知识产权）
│   ├── ymos-diagnosis/            # 投资策略诊断模块（独立可用）
│   │   ├── SKILL.md               # 诊断入口（问诊 + 体检）
│   │   ├── README.md              # 模块说明（Agent 导航）
│   │   └── knowledge/             # 投资公理框架 + 诊断案例库
│   ├── 策略分析/                  # 按月归档：报告 + 日志 + Raw_Data/
│   ├── 交易计划/                  # ← 运行时生成（Console 交易计划台产出）
│   └── 决策审计/                  # ← 运行时生成（Console 决策留痕）
│
├── 持仓与关注/                    # 状态层
│   ├── SOP_入职引导.md            # 首次使用引导（开始使用）
│   ├── SOP_持仓收口.md            # 每日收尾（视图 + 校验 + Dashboard）
│   ├── SOP_标的管理.md            # 关注/建仓/清仓
│   ├── SOP_周度复盘.md            # 每周：账户曲线定下周基调 + 熔断判定
│   ├── 持仓_状态机.md             # 持仓真相源
│   ├── Watchlist_状态机.md        # 关注真相源
│   ├── 当前关注方向与投资偏好.md  # 用户投资策略（灵魂文件）
│   ├── 账户资金曲线.md            # 账户级状态真相源（模板）
│   ├── 击球区注册表.md            # 买点条件的机器可读真相源（模板）
│   ├── 主线首发信号跟踪.md        # 板块首发信号与代表权（模板）
│   ├── 持仓备忘录_视图.md         # ← 运行时生成（持仓收口产出）
│   ├── dashboard/                 # ← 运行时生成（可视化看板 HTML）
│   ├── 持仓/                      # 个股文件夹（= 分析上下文）
│   └── 动态Watchlist/             # 关注标的文件夹
│
├── Agents/                        # 运行时编排层（V4 新增）
│   ├── README.md                  # 先读这个：4 角色 + 两条硬规矩
│   ├── ORCHESTRATION.md           # 角色完整定义（读什么/写什么/禁止什么）
│   ├── EXECUTION_PLAYBOOK.md      # 每角色五段式执行手册 + 异常处理
│   ├── RUNTIME_MODE.md            # 单主控 vs 真并发，及升级条件
│   ├── SCHEDULES.md               # 定时任务链与依赖
│   └── *-agent.md                 # 四张角色卡（供 Agent 快速加载）
│
├── BrainStorm/                    # 思考沉淀层（V4 新增）
│   ├── README.md                  # 先读这个：三层架构与升级规则
│   ├── SOP_头脑风暴.md            # 工作流与暗号
│   ├── MEMORY.md                  # 长期记忆（AI 每次必读）
│   ├── 每日头脑风暴/Raw_Thoughts/ # 原始想法，按月归档
│   ├── Summarized_Insights/       # AI 整理总结，按月归档
│   ├── 投资感悟归档/              # 短期灵感 → 长期框架 → 执行清单
│   ├── 状态机/STATUS.md           # 当前阶段状态
│   └── scripts/                   # 上下文加载器（零依赖）
│
├── Console/                       # Reader + 决策台（V4 新增，人在用）
│   ├── README.md                  # 怎么用、怎么接 vault、怎么换规则与目录分组
│   ├── server.py                  # 单端口本地服务（stdlib 零依赖）
│   ├── config.example.json        # → 复制成 config.json：路径 + Reader 目录对照表
│   ├── rules.example.json         # → 复制成 rules.json 换自己的门禁口径
│   ├── index.html                 # 导航页
│   ├── reader.html                # 日常报告浏览器（只读）
│   ├── 交易计划台.html            # 盘后：定调 → 审持仓 → 列明日动作
│   └── 新高买卖决策台.html        # 盘中：全绿才允许扣扳机
│
├── 总入口暗号.md                  # 所有暗号路由表
├── AGENT_GUIDE.md                 # Agent 操作手册
├── 进阶指南.md                    # 数据源升级 + 策略定制 + 扩展
├── CHANGELOG.md                   # 版本历史（唯一真相源）
└── README.md                      # 本文件
```

---

## 可扩展性与改装潜力

YMOS 不是一个封闭产品，而是一个**开放框架**。它的扩展性体现在四个层面：

### 数据层：接更多数据源

`Eyes/scripts/` 下的每个脚本都是独立的原子能力。接入新数据源就是写一个新的 `fetch_xxx.py`，输出标准 JSON，SOP 自动识别。价格路由器已经实现了 Finnhub → Tushare → Yahoo 的三源自动分流，增加第四源只需加一个分支。

### 处理层：改造 P 系列提示词

`Brain/references/` 下的 P1-P18 是**框架样板**。它们代表一种经过验证的投资决策路由——但你完全可以根据自己的投资风格改造它们：调整 P5 FOMO Killer 的评分权重、在 P1 里加入行业特定指标、改变 P12 纪律审查的红线规则。**这才是最核心的进阶——不是接更多数据，而是让系统的大脑越来越像你自己的投资直觉。**

### 路由层：增加新的暗号和工作流

现有 SOP 和策略路由不是上限。你可以在 SOP 中增加条件分支、新增路由类型、甚至设计全新的工作流。所有编排逻辑都在 Markdown 里，不需要改代码。

### 能力层：Agent Skills 与跨模块联动

YMOS 的 SOP 天然兼容 Agent 的外挂能力。当 Agent 有搜索能力，P1 建档自动补足公开信息；当 Agent 挂载了财经数据工具（MX_FinSearch、MX_FinData 等），选股筛选和财务快照自动增强；当同一目录里有其他知识模块（你的头脑风暴、研究笔记），它们自动成为分析上下文。能力越强，SOP 的执行质量越高——但即使什么都没有，纯 RSS + Yahoo Finance 也能完整跑通。

### 系统是养出来的

YMOS 不是装上就完美的成品——它更像一个**需要你跟 AI 一起养大的系统**。

第一次跑 SOP 的时候，你可能会遇到输出不够理想、某个环节卡住、报告格式不顺眼的情况。这是正常的。每次跑通一轮，你就能发现可以优化的地方：调 P 系列提示词的参数、改 SOP 的条件分支、增加新的数据源。跑得越多，系统越懂你。

`Eyes/scripts/` 下的现有脚本，既是系统运行的功能依赖，也是你**改造和扩展的参考示例**。想接一个新的 RSS 源？参考 `fetch_rss.py`。想加一个价格数据源？参考 `fetch_price_api.py` 的接口模式。每个脚本都是独立的、零依赖的原子能力——照着写一个新的就行。

> 详细的数据源配置、策略定制方法、Agent 扩展协议见 [`进阶指南.md`](进阶指南.md)。
> Agent 的权限规则、能力整合边界、写回规则见 [`AGENT_GUIDE.md`](AGENT_GUIDE.md) — **如果你是 AI Agent，请优先阅读此文件。**

---

## V3 → V4：升级了什么

> V3（2026-03）是首个完整公开版本，三模块架构已经能跑通。
> V4 在真实市场里又跑了四个月之后，把研究深度、可选数据层、执行纪律和认知复盘接回同一个系统。

| 维度 | V3 | V4 | 为什么重要 |
|:---|:---|:---|:---|
| **定位** | 一套能跑的投研系统 | 一个**能装下你自己策略的框架** | V3 里策略和框架是半焊死的；V4 把它们掰开，你带自己的策略进来 |
| **深度研究** | P1-P16，偏建档、估值和策略路由 | **P17 七问 + P9/P17 单标的专项调研** | 把“是不是好公司”“应该值多少”“现在处于哪一段”“是否适合持有”拆开回答 |
| **数据层** | RSS、Finnhub、Tushare、Yahoo | **官方问财 Skill + YMOS 适配层** | A 股筛选、财务、公告、研报和新闻可以渐进增强；没装 Skill 也不阻塞基础主链 |
| **编排** | 暗号触发，顺序靠自觉 | **Agents 运行时**：4 角色 + 调度链 + 状态写回唯一出口 | 行情一急最容易跳步；把顺序写死，等于装了一道结构性减速带 |
| **阅读与执行** | 报告散在不同目录，系统给结论、人自己动手 | **Reader + Console 决策台**：报告聚合、门禁、计划和决策留痕 | 大部分亏损不来自判断错，来自信息翻不到或**知道了但没照做** |
| **纪律** | P12 单点纪律审查 | 账户曲线 + 周度复盘 + 熔断 + 击球区注册表 | 单票纪律管不住账户级风险；连续不盈利是风偏问题，不是选股问题 |
| **认知进化** | 复盘结果容易停在一篇报告里 | **BrainStorm**：Raw → Insight → 原则 → 回写偏好 | 只有被写回并在下次决策中调用的感悟，才真正改变行为 |
| **可复用性** | 抄框架 + 半抄作者的策略 | 抄**把自己策略代码化的方法** | 你的能力圈、周期、风险偏好和别人不一样，规则就不该一样 |

**一句话总结**：V3 让 AI 帮你把信息处理完、把分析跑完；
V4 再往前一步——**让系统在你想违背自己规矩的那一刻，把你拦下来，并且留下记录。**

---

## 常见问题

**Q：已经有旧版本了，怎么最快用上 V4？**

先备份旧目录，再下载 V4 并运行入职引导（`开始使用`）。V1 用户可以手动把旧研报和笔记移入 V4 的个股文件夹；V3 用户请先阅读 [UPGRADE.md](UPGRADE.md)，尤其不要直接覆盖自己的持仓、关注、偏好和本地配置文件。

**Q：需要会写代码吗？**

不需要。所有操作通过自然语言暗号触发，配置通过 Markdown 文件修改。唯一可能需要终端操作的是配置 `.env` 环境变量（填 API Key），但这也不是必须的——零配置就能跑通最小闭环。

**Q：支持哪些市场？**

美股、A 股、Crypto 都支持。价格路由器会根据 ticker 格式自动判断走 Finnhub（美股/Crypto）、Tushare（A股）还是 Yahoo Finance（兜底）。板块关联逻辑支持跨市场映射（如"英伟达财报 → A股光模块"）。

**Q：AI 会不会替我做交易决策？**

不会。YMOS 严格遵循 **Human in the Loop** 原则。所有策略建议都要经过 P12 纪律审查，审查结果只是"建议"——最终按不按按钮、下不下单，永远是你自己的事。

**Q：P 系列提示词能改吗？**

能，而且鼓励你改。`Brain/references/` 下的 P1-P18 是框架样板，你可以根据自己的投资风格调整权重、修改红线规则、增加行业特定指标。**让系统的大脑越来越像你自己的投资直觉，才是最核心的进阶。**

**Q：推荐用什么 Agent 工具来运行 YMOS？**

YMOS 可以在任何支持文件读写和代码执行的 AI Agent 环境中运行。核心要求只有两个：**能读写本地文件** + **能运行 Python 脚本**。满足这两点，YMOS 就能跑。

但「能跑」和「值得当总管」是两件事。这里要分清两个角色：

**① 统筹层：一个负责记忆与协调的常驻 Agent 框架（建议只选一个）**

- **推荐：[Hermes](https://hermes-agent.nousresearch.com/)**（Nous Research 开源，[GitHub](https://github.com/NousResearch/hermes-agent)）——
  本地运行、数据不出本机，支持文件读写、代码执行、定时任务和持久化记忆。
  作者目前自己的 YMOS 就是以 Hermes 作为整体的 Agent 框架跑的：**长期记忆、角色编排、任务串联都收在这一层**。
- 也可以用 [OpenClaw](https://openclaw.com/)（龙虾）——同样是本地优先、支持定时任务和 subagent 编排，云端 Claw 系列原理相同。

**统筹层为什么建议只选一个**：YMOS 的价值来自状态机、账户曲线和长期记忆的**连续性**。
两个框架各记一半，你就有了两份互相不知道对方存在的记忆——这比没有记忆更糟。

**② 执行层：按单个对话／单个任务调用的 Agent（可以多个并存）**

- **Claude Code / Claude Cowork、ChatGPT Codex** —— 订阅会员产品，文件操作和定时任务都很成熟。
  作者的实际用法是：**部分定时任务 routing 给 Claude Code 或 Codex 分担**，跑完把产出写回 YMOS 目录，
  统筹层照常读文件，互不冲突。
- **本文档和进阶指南里推荐的 Agent 工具，实际用下来都能跑通 YMOS**——
  按单个对话使用的场景（跑一次深研、改一份 SOP、调一个脚本），随便挑顺手的即可。

> 一句话：**统筹记忆与协调用 Hermes，具体任务谁顺手用谁。**
> 执行层换来换去没关系，因为真相源始终是磁盘上那些 Markdown，不是某个 Agent 的会话上下文。

**Q：怎么最快开始使用？**

三步：① 下载项目放到本地目录 → ② 告诉你的 AI Agent `请读取 [YMOS路径]/总入口暗号.md` → ③ 说「开始使用」，Agent 会引导你走完入职流程（设置投资偏好、初始化持仓）。入职完成后，说「跑一下市场洞察」就能看到第一份报告。

**Q：报告在哪里看？**

所有报告都是 Markdown 文件，按日期归档在本地目录里：
- 市场洞察：`Eyes/市场洞察/YYYY-MM/`
- 投资雷达：`Eyes/投资雷达/YYYY-MM/`
- 策略分析：`Brain/策略分析/YYYY-MM/`
- 持仓备忘录视图：`持仓与关注/持仓备忘录_视图.md`
- 可视化 Dashboard：`持仓与关注/dashboard/YYYY-MM/`（HTML 文件，浏览器打开）

推荐用 [Obsidian](https://obsidian.md) 日常浏览——它是本地存储的 Markdown 笔记工具，阅读报告和管理知识库很方便。

**Q：模型选择有什么建议？**

**永远尽可能选当下最强的稳定模型。** YMOS 的核心价值在 SOP + P 系列提示词的分析质量，这完全取决于底层模型的能力。如果资源有限，**优先把最好的模型用在策略分析环节**（Brain 模块），这里的判断质量对最终决策影响最大。具体模型名称更新很快，以你的 Agent 宿主当时可用的型号为准。

**Q：数据安全呢？所有数据都在本地吗？**

YMOS 的报告、状态机和知识库默认都保存为你本地目录里的 Markdown 文件，没有 YMOS 自建云端数据库。但如果你启用云端 Agent、Web 搜索或 Finnhub / Tushare / 问财等外部 API，对应的提示词、查询条件或 ticker 会按这些服务自身的隐私规则发送给服务商。真实 Key 只放本地 `.env`，不要写进 Markdown、脚本、聊天或 Git。

**Q：必须用 Obsidian 吗？**

不是必须的。YMOS 运行时不依赖 Obsidian 软件——它只需要一个本地文件目录。你可以把 YMOS 文件夹放在任何你觉得方便的位置。但我们推荐搭配 [Obsidian](https://obsidian.md) 使用：它是一个本地存储的 Markdown 笔记工具，用来日常阅读报告、浏览个股知识库、管理整个文档体系非常方便，而且同样是本地存储，跟 YMOS 的数据安全理念一致。

**Q：用 Obsidian 打开项目后，看不到 Python 脚本文件？**

这是正常的。Obsidian 是 Markdown 笔记工具，默认只显示它能渲染的文件类型（`.md`、图片等），不会显示 `.py`、`.json`、`.env` 等代码文件。但这些文件确实存在于项目目录中——你的 AI Agent 可以正常读取和执行它们，不影响 YMOS 的任何功能。

如果你需要查看或编辑代码文件（比如配置 `.env`、查看脚本逻辑），可以用以下任一 IDE 工具打开 YMOS 文件夹：

- [VS Code](https://code.visualstudio.com/) — 微软出品，免费，最主流的代码编辑器
- [Trae](https://www.trae.ai/) — 字节跳动出品，免费，内置 AI 编程助手
- [Google Antigravity](https://antigravity.google/) — Google 出品，免费，内置 Gemini AI

用 IDE 打开同一个 YMOS 文件夹，你就能看到完整的项目结构，包括 `Eyes/scripts/` 下的所有 Python 脚本。日常使用推荐 Obsidian 看报告 + IDE 看代码，各司其职。

---

## 系统的本质

YMOS 不帮你赚钱——它帮你**不犯低级错误**。

把信息焦虑交给 Eyes，把分析苦力交给 Brain，把状态记忆交给持仓与关注。你腾出来的时间和精力，用来做投资里真正值钱的事：

1. **打磨策略** — 优化你的判断框架，在关键时刻保持纪律
2. **做关键判断** — AI 给建议，你盖章；系统提供决策支持，不替你决策
3. **深度调研** — 自己读研报、跟踪行业、理解公司基本面，把挖到的材料手动拖入个股文件夹

第 3 点尤其重要：AI 能帮你建档和跑分析链路，但**真正的认知差来自你对公司的深度理解**。个股文件夹里你手动放入的研报、笔记、行业资料，都会自动成为 AI 后续分析的上下文——你研究得越深，AI 的输出质量越高。

框架搭好了，能力升级是渐进的——技术配置让手脚更灵活，策略进化让大脑更聪明。

**你的系统，你做主。**

---

## 文档导航

| 文档 | 面向 | 定位 |
|:---|:---|:---|
| [README.md](README.md)（本文件） | 所有人 | 架构总览、上手指南、暗号速查 |
| [AGENT_GUIDE.md](AGENT_GUIDE.md) | AI Agent | 权限规则、依赖关系、能力整合、写回规则 |
| [进阶指南.md](进阶指南.md) | 进阶用户 | 数据源升级、策略定制、系统扩展 |
| [UPGRADE.md](UPGRADE.md) | V3 用户 | 保留个人数据并升级到 V4 |
| [总入口暗号.md](总入口暗号.md) | Agent + 用户 | 完整暗号路由表 + SOP 链接 |
| [Console/README.md](Console/README.md) | 所有人 | Reader 与两个决策台怎么用、怎么接自己的 vault、怎么换目录分组和规则 |
| [Agents/README.md](Agents/README.md) | Agent + 进阶用户 | 4 个角色的职责、顺序、边界 |
| [CHANGELOG.md](CHANGELOG.md) | 所有人 | 版本历史（唯一真相源） |

协同规则：`README.md` 只讲**当前版本怎么用**；`CHANGELOG.md` 记录**每个版本改了什么**；`UPGRADE.md` 只解决**旧版本怎么迁移**。三者互相链接，但不要把完整更新日志复制进 README。

---

## 许可证

本项目采用 [CC BY-NC 4.0](https://creativecommons.org/licenses/by-nc/4.0/) 许可证。

- 个人使用、学习、研究：自由使用
- 公开发布衍生作品：请注明来源
- 商业用途：需要单独授权

---

## 关于作者

**勇麦（Yongmai）** — AI + 投资实践者 / 个人投资者

用 AI 增强投资决策，用内容沉淀信任。YMOS 是我自用的投资操作系统，现在开源给所有愿意用系统思维做投资的人。

- 🌐 博客：[yongmai.xyz](https://yongmai.xyz)
- 📧 邮箱：evan@yongmai.xyz
- 📱 公众号：勇麦
- 🎬 抖音：勇麦

---

## 参与贡献

如果你觉得 YMOS 对你有帮助：

- ⭐ **Star** — 让更多人看到这个项目
- 🍴 **Fork** — 基于你的投资风格自由魔改（改 P 系列提示词、加数据源、调路由逻辑）
- 🐛 **Issue** — 发现 bug 或有改进建议，欢迎提 Issue
- 🤝 **PR** — 有好的想法直接提 Pull Request，一起完善

YMOS 的核心价值不在代码量，而在投资思路的结构化。欢迎把你的投资智慧沉淀进系统里。

---

*YMOS V4.0 · 2026-07-27 · 轻脚本 + 重 SOP + 软路由 + 运行时编排 + 决策闸门*
