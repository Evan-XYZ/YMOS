# 升级指南：V3 → V4

**先说结论：V4 是纯增量升级，不需要重装，也不会动你的数据。**

V4 做的事情是**加了三个新目录**（`Console/` `Agents/` `BrainStorm/`）+ 改了文档和 SOP。
它**没有改动任何一个存放你个人数据的文件**——你的投资偏好、状态机、个股文件夹、报告归档，
在 V4 的提交里一行都没碰。

---

## 你的数据在哪（升级前先认清这几个）

| 文件 / 目录 | 里面是什么 | 仓库里跟踪吗 |
|:---|:---|:---|
| `持仓与关注/当前关注方向与投资偏好.md` | **你的投资灵魂文件** | ✅ 跟踪（仓库里是空模板） |
| `持仓与关注/持仓_状态机.md` | 你的持仓真相源 | ✅ 跟踪（仓库里是空模板） |
| `持仓与关注/Watchlist_状态机.md` | 你的关注真相源 | ✅ 跟踪（仓库里是空模板） |
| `持仓与关注/持仓备忘录_视图.md` | 收口生成的状态快照 | ✅ 跟踪（仓库里是空模板） |
| `持仓与关注/持仓/*/`、`动态Watchlist/*/` | 个股文件夹 | ❌ 已忽略，安全 |
| `Eyes/市场洞察/`、`投资雷达/`、`Brain/策略分析/` 下的年月目录 | 历史报告 | ❌ 已忽略，安全 |
| `.env` | 你的 API Key | ❌ 已忽略，安全 |
| `Eyes/scripts/rss_sources_custom.json` | 你自定义的 RSS 源 | ❌ 已忽略，安全 |

**前四个是唯一需要留意的**——它们在仓库里以空模板形式被跟踪，而你本地已经填满了内容。

---

## 方式一：用 git（推荐）

```bash
cd 你的YMOS目录
git stash            # 如果你改过被跟踪的文件，先暂存
git pull
git stash pop        # 把你的改动放回来
```

**为什么这样是安全的**：V4 的提交**没有修改**上面那四个文件。
git 只在「你改了 A，上游也改了 A」时才产生冲突——这里上游没动，所以你的内容会原样保留。

如果 `git stash pop` 提示冲突，只可能出现在这两个文件上：

| 文件 | 冲突原因 | 怎么办 |
|:---|:---|:---|
| `Eyes/scripts/rss_sources.json` | V4 改了描述里一个过期版本串 | 保留你的版本即可，或改用 `rss_sources_custom.json` |
| `.env.example` | V4 改了顶部版本注释 | 直接用上游版本；你的真实配置在 `.env` 里，不受影响 |

> 更稳妥的做法：升级前先 `cp -r 持仓与关注 ~/ymos-backup-持仓与关注` 备份一份。**一分钟的事，省掉所有心理负担。**

---

## 方式二：下载 ZIP 覆盖（⚠️ 这里最容易出事）

如果你不用 git，而是下载 ZIP 解压覆盖，**直接覆盖会清空你的投资偏好和状态机**——
因为仓库里那四个文件是空模板。

正确姿势：

```bash
# 1. 先备份你的数据
cp -r 持仓与关注 ~/ymos-backup

# 2. 解压新版到一个独立目录（不要直接覆盖）
unzip YMOS-V4.zip -d ~/ymos-v4-new

# 3. 只把「新增的」和「框架类的」拷过来
cp -r ~/ymos-v4-new/Console      ./
cp -r ~/ymos-v4-new/Agents       ./
cp -r ~/ymos-v4-new/BrainStorm   ./
cp -r ~/ymos-v4-new/Brain/references/*      ./Brain/references/
cp    ~/ymos-v4-new/Brain/SOP_*.md          ./Brain/
cp    ~/ymos-v4-new/Eyes/SOP_*.md           ./Eyes/
cp -r ~/ymos-v4-new/Eyes/scripts/*          ./Eyes/scripts/
cp    ~/ymos-v4-new/持仓与关注/SOP_*.md      ./持仓与关注/
cp    ~/ymos-v4-new/*.md                    ./          # README / 暗号 / 指南 / CHANGELOG

# 4. 这四个只在「你确认自己是全新安装」时才拷
#    ❌ 当前关注方向与投资偏好.md
#    ❌ 持仓_状态机.md
#    ❌ Watchlist_状态机.md
#    ❌ 持仓备忘录_视图.md
```

> ⚠️ **注意第 4 步的三个模板文件是新增的**，可以放心拷：
> `账户资金曲线.md`、`击球区注册表.md`、`主线首发信号跟踪.md` —— V3 里没有它们。

---

## 升级后要做的三件事

### 1. 启用 Console（Reader + 决策台）

```bash
cd Console
cp config.example.json config.json     # 编辑它，指向你的 vault
python3 server.py
```

打开 <http://localhost:5273>。Reader、交易计划台和新高买卖决策台共用这一个端口；
零依赖，不用装任何包。

### 2. 起用 BrainStorm（可选但建议）

对 AI 说「开始今天的头脑风暴」即可。目录已经建好，不需要额外配置。

### 3. 让 Agent 重读新文档

新会话开头告诉它：

```
先读 总入口暗号.md 和 AGENT_GUIDE.md，我升级到 V4 了
```

V4 新增了 `Agents/` 运行时协议、`BrainStorm/` 暗号、以及若干新 SOP，
Agent 需要重新加载路由表才知道这些新能力。

---

## V4 新增了什么（快速一览）

| 目录 / 文件 | 是什么 |
|:---|:---|
| `Console/` | 单端口可视层：Reader + 交易计划台 + 新高买卖决策台 + 决策留痕 |
| `Agents/` | 运行时编排：4 角色 + 调度链 + 执行手册 |
| `BrainStorm/` | 思考沉淀：Raw → Insight → 长期原则 |
| `持仓与关注/SOP_周度复盘.md` | 账户曲线定下周基调 + 熔断 |
| `持仓与关注/账户资金曲线.md` | 账户级状态真相源（模板） |
| `持仓与关注/击球区注册表.md` | 买点条件的机器可读真相源（模板） |
| `持仓与关注/主线首发信号跟踪.md` | 板块首发信号与代表权（模板） |
| `Brain/references/p17` `p18` | 核心资产判定七问、每日异动扫描 |
| `Brain/SOP_单标的P9P17专项调研.md` | 人工点名标的的深研入口 |
| `CHANGELOG.md` | 版本历史（新增，唯一真相源） |

完整清单见 [CHANGELOG.md](CHANGELOG.md)。

---

## 常见问题

**Q：我 V3 用得好好的，非升不可吗？**
不是。V3 的三模块链路在 V4 里一行没改，你可以继续用。
V4 加的是**执行层的闸门**（Console）和**沉淀层**（BrainStorm）——
如果你觉得自己"知道该怎么做但盘中总是做不到"，那 V4 才对你有意义。

**Q：升级会不会影响我的定时任务？**
不会。原有的四步闭环暗号（市场洞察 / 投资雷达 / 策略分析 / 收口）完全不变。
V4 新增的暗号是加法，不覆盖旧的。

**Q：我改过 P 系列提示词，升级会覆盖吗？**
会。`Brain/references/` 下的提示词是被跟踪的框架文件。
如果你按自己的风格改造过（这是被鼓励的），升级前先备份，或者用 git 的 `stash/pop` 流程。
**长期建议**：把你的个性化改动记录在 `BrainStorm/投资感悟归档/长期框架/` 里，
这样即使文件被覆盖，你的思路还在。

**Q：从 V1 怎么升到 V4？**
V1 归档在 [`v1-legacy` 分支](https://github.com/Evan-XYZ/YMOS/tree/v1-legacy)，架构差异太大，
建议**全新安装 V4**，然后手动把你的投资偏好和持仓清单填进去（跑一次 `开始使用` 引导即可）。

---

*2026-07-27 · YMOS V4*
